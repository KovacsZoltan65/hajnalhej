diff -ruN /mnt/data/hajnalhej_locale_original/.env /mnt/data/hajnalhej_locale/.env
--- /mnt/data/hajnalhej_locale_original/.env	2026-04-27 09:12:18.000000000 +0000
+++ /mnt/data/hajnalhej_locale/.env	2026-05-05 07:52:56.000000000 +0000
@@ -7,6 +7,7 @@
 APP_LOCALE=en
 APP_FALLBACK_LOCALE=en
 APP_FAKER_LOCALE=en_US
+APP_TIMEZONE=UTC
 
 APP_MAINTENANCE_DRIVER=file
 # APP_MAINTENANCE_STORE=database
@@ -65,7 +66,6 @@
 
 VITE_APP_NAME="${APP_NAME}"
 
-
 INERTIA_SSR_ENABLED=true
 INERTIA_SSR_RUNTIME="node"
 INERTIA_SSR_ENSURE_RUNTIME_EXISTS=false
diff -ruN /mnt/data/hajnalhej_locale_original/app/Providers/AppServiceProvider.php /mnt/data/hajnalhej_locale/app/Providers/AppServiceProvider.php
--- /mnt/data/hajnalhej_locale_original/app/Providers/AppServiceProvider.php	2026-04-27 07:39:18.000000000 +0000
+++ /mnt/data/hajnalhej_locale/app/Providers/AppServiceProvider.php	2026-05-05 08:04:34.000000000 +0000
@@ -36,7 +36,10 @@
 use App\Support\PermissionRegistry;
 use Illuminate\Auth\Middleware\RedirectIfAuthenticated;
 use Illuminate\Support\Facades\Gate;
+use Illuminate\Support\Facades\RateLimiter;
 use Illuminate\Support\ServiceProvider;
+use Inertia\Inertia;
+use Session;
 use Spatie\Activitylog\Models\Activity;
 use Spatie\Permission\Models\Role;
 use Spatie\Permission\Models\Permission;
@@ -76,7 +79,7 @@
 
         Gate::define(
             'viewProcurementIntelligence',
-            static fn (User $user): bool => app(ProcurementIntelligencePolicy::class)->viewAny($user),
+            fn (User $user): bool => app(ProcurementIntelligencePolicy::class)->viewAny($user),
         );
 
         Gate::policy(Category::class, CategoryPolicy::class);
@@ -96,5 +99,25 @@
         Gate::policy(Supplier::class, SupplierPolicy::class);
         Gate::policy(User::class, UserPolicy::class);
         Gate::policy(WeeklyMenu::class, WeeklyMenuPolicy::class);
+
+        $app_locale = app()->getLocale();
+        $csrf_token = csrf_token();
+
+        Inertia::share([
+            "errors" => fn (): array => Session::get('errors') 
+                ? Session::get('errors')->getBag('default')->getMessages() 
+                : (object) [],
+            "available_locales" => config('app.available_locales'),
+            "locale" => fn (): string => $app_locale,
+            "preferences" => fn (): array => [
+                'locale' => $app_locale,
+                'timezone' => Session::has('timezone') ? Session::get('timezone') : config('app.timezone', 'UTC'),
+                'theme' => Session::has('theme') ? Session::get('theme') : 'system',
+            ],
+        ]);
+
+        Inertia::share('flash', fn () => ['message' => Session::get('message'),] );
+
+        Inertia::share('csrf_token', fn () => $csrf_token);
     }
 }
diff -ruN /mnt/data/hajnalhej_locale_original/config/app.php /mnt/data/hajnalhej_locale/config/app.php
--- /mnt/data/hajnalhej_locale_original/config/app.php	2026-04-27 15:21:36.000000000 +0000
+++ /mnt/data/hajnalhej_locale/config/app.php	2026-05-05 07:53:36.000000000 +0000
@@ -65,7 +65,7 @@
     |
     */
 
-    'timezone' => 'UTC',
+    'timezone' => config('APP_TIMEZONE', 'UTC'),
 
     /*
     |--------------------------------------------------------------------------
diff -ruN /mnt/data/hajnalhej_locale_original/lang/en.json /mnt/data/hajnalhej_locale/lang/en.json
--- /mnt/data/hajnalhej_locale_original/lang/en.json	2026-05-04 14:49:32.000000000 +0000
+++ /mnt/data/hajnalhej_locale/lang/en.json	2026-05-05 07:34:54.000000000 +0000
@@ -19,6 +19,10 @@
     "nav.audit_logs": "Audit Logs",
     "brand.name": "Hajnalhéj",
     "brand.slogan": "Crispy mornings.",
+    "brand.description": "Sourdough cakes and breads in Budapest",
+
+    "all_right": "All rights reserved",
+    "bakery": "Bakery",
 
     "nav.overview": "Overview",
     "nav.dashboard": "Daily overview",
@@ -286,6 +290,8 @@
     "common.logout": "Logout",
     "common.type": "Type",
     "common.name": "Name",
+    "common.slug": "Slug",
+    "common.automatic_generated": "Automatically generated based on name",
 
     "weekly_menu.current": "Current weekly menu",
     "weekly_menu.fallback_notice": "There is no active weekly menu for today, so the most recently published menu is shown.",
@@ -345,6 +351,7 @@
     "admin_categories.category_created": "Category created",
     "admin_categories.category_updated": "Category updated",
     "admin_categories.category_deleted": "Category deleted",
+    "admin_categories.active_category": "Aktíve category",
 
     "admin_ingredients.meta_title": "Ingredients",
     "admin_ingredients.eyebrow": "Admin / Ingredients",
@@ -371,6 +378,7 @@
     "admin_ingredients.material_updated": "Material updated",
     "admin_ingredients.material_deleted": "Material deleted",
     "admin_ingredients.material_added": "Recipe item added",
+    "admin_ingredients.active_ingredient": "Active ingredient",
 
     "admin_supplier.created": "Supplier created",
     "admin_supplier.updated": "Supplier updated",
@@ -449,6 +457,7 @@
     "admin_procurement_intelligence.columns.quantity": "Quantity",
     "admin_procurement_intelligence.columns.current_stock": "Current stock",
     "admin_procurement_intelligence.columns.minimum_stock": "Minimum stock",
+    "admin_procurement_intelligence.reorder_level": "reorder level",
     "admin_procurement_intelligence.columns.weekly_average_consumption": "Weekly average consumption",
     "admin_procurement_intelligence.columns.recommended_supplier": "Recommended supplier",
     "admin_procurement_intelligence.columns.lead_time": "Lead time",
diff -ruN /mnt/data/hajnalhej_locale_original/lang/hu.json /mnt/data/hajnalhej_locale/lang/hu.json
--- /mnt/data/hajnalhej_locale_original/lang/hu.json	2026-05-04 14:49:24.000000000 +0000
+++ /mnt/data/hajnalhej_locale/lang/hu.json	2026-05-05 07:36:08.000000000 +0000
@@ -19,6 +19,10 @@
     "nav.audit_logs": "Naplók",
     "brand.name": "Hajnalhéj",
     "brand.slogan": "Ropogós reggelek.",
+    "brand.description": "Kovászos sütemények és kenyerek Budapesten",
+
+    "all_right": "Minden jog fenntartva",
+    "bakery": "Pékség",
 
     "nav.overview": "Áttekintés",
     "nav.dashboard": "Napi áttekintés",
@@ -276,6 +280,9 @@
     "common.logout": "Kijelentkezés",
     "common.type": "Típus",
     "common.name": "Név",
+    "common.slug": "Slug",
+    "common.automatic_generated": "Automatikusan generálódik a név alapján",
+    "common.sku": "Cikkszám",
 
     "weekly_menu.current": "Aktuális heti menü",
     "weekly_menu.fallback_notice": "Jelenleg nincs aktív heti menü a mai napra, ezért a legutóbb publikált menü látható.",
@@ -338,6 +345,7 @@
     "admin_categories.category_created": "Kategória létrehozva",
     "admin_categories.category_updated": "Kategória frissítve",
     "admin_categories.category_deleted": "Kategória törölve",
+    "admin_categories.active_category": "Aktív kategória",
 
     "admin_ingredients.meta_title": "Alapanyagok",
     "admin_ingredients.eyebrow": "Admin / Alapanyagok",
@@ -364,6 +372,7 @@
     "admin_ingredients.material_updated": "Alapanyag frissítve",
     "admin_ingredients.material_deleted": "Alapanyag törölve",
     "admin_ingredients.material_added": "Recipe item added",
+    "admin_ingredients.active_ingredient": "Aktív alapanyag",
 
     "admin_supplier.created": "Beszállító létrehozva",
     "admin_supplier.updated": "Beszállító frissítve",
@@ -441,6 +450,7 @@
     "admin_procurement_intelligence.columns.weighted_average": "Súlyozott átlag",
     "admin_procurement_intelligence.columns.current_stock": "Aktuális készlet",
     "admin_procurement_intelligence.columns.minimum_stock": "Minimum készlet",
+    "admin_procurement_intelligence.reorder_level": "utánrendelési szint",
     "admin_procurement_intelligence.columns.weekly_average_consumption": "Heti átlagfogyás",
     "admin_procurement_intelligence.columns.recommended_supplier": "Ajánlott beszállító",
     "admin_procurement_intelligence.columns.lead_time": "Lead time",
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Categories/CategoryForm.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Categories/CategoryForm.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Categories/CategoryForm.vue	2026-04-22 12:55:40.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Categories/CategoryForm.vue	2026-05-05 07:12:20.107389760 +0000
@@ -1,10 +1,10 @@
 <script setup>
-import { watch } from 'vue';
-import InputNumber from 'primevue/inputnumber';
-import InputText from 'primevue/inputtext';
-import Textarea from 'primevue/textarea';
-import ToggleSwitch from 'primevue/toggleswitch';
-import { slugify } from '../../../Utils/slugify';
+import { watch } from "vue";
+import InputNumber from "primevue/inputnumber";
+import InputText from "primevue/inputtext";
+import Textarea from "primevue/textarea";
+import ToggleSwitch from "primevue/toggleswitch";
+import { slugify } from "../../../Utils/slugify";
 
 const props = defineProps({
     form: {
@@ -17,41 +17,80 @@
     () => props.form.name,
     (name) => {
         props.form.slug = slugify(name);
-    },
+    }
 );
 </script>
 
 <template>
     <div class="grid gap-4 md:grid-cols-2">
         <div class="space-y-2 md:col-span-2">
-            <label for="category-name" class="text-sm font-medium text-bakery-dark">Név</label>
-            <InputText id="category-name" v-model="form.name" class="w-full" :invalid="Boolean(form.errors.name)" />
-            <p v-if="form.errors.name" class="text-xs text-red-700">{{ form.errors.name }}</p>
+            <label for="category-name" class="text-sm font-medium text-bakery-dark">{{
+                $t("common.name")
+            }}</label>
+            <InputText
+                id="category-name"
+                v-model="form.name"
+                class="w-full"
+                :invalid="Boolean(form.errors.name)"
+            />
+            <p v-if="form.errors.name" class="text-xs text-red-700">
+                {{ form.errors.name }}
+            </p>
         </div>
 
         <div class="space-y-2 md:col-span-2">
-            <label for="category-slug" class="text-sm font-medium text-bakery-dark">Slug</label>
+            <label for="category-slug" class="text-sm font-medium text-bakery-dark">{{
+                $t("common.slug")
+            }}</label>
             <InputText id="category-slug" v-model="form.slug" class="w-full" disabled />
-            <p class="text-xs text-bakery-dark/60">Automatikusan generálódik a név alapján.</p>
-            <p v-if="form.errors.slug" class="text-xs text-red-700">{{ form.errors.slug }}</p>
+            <p class="text-xs text-bakery-dark/60">
+                {{ $t("common.automatic_generated") }}.
+            </p>
+            <p v-if="form.errors.slug" class="text-xs text-red-700">
+                {{ form.errors.slug }}
+            </p>
         </div>
 
         <div class="space-y-2 md:col-span-2">
-            <label for="category-description" class="text-sm font-medium text-bakery-dark">Leírás</label>
-            <Textarea id="category-description" v-model="form.description" rows="4" class="w-full" auto-resize />
-            <p v-if="form.errors.description" class="text-xs text-red-700">{{ form.errors.description }}</p>
+            <label
+                for="category-description"
+                class="text-sm font-medium text-bakery-dark"
+                >{{ $t("admin_permissions.fields.description") }}</label
+            >
+            <Textarea
+                id="category-description"
+                v-model="form.description"
+                rows="4"
+                class="w-full"
+                auto-resize
+            />
+            <p v-if="form.errors.description" class="text-xs text-red-700">
+                {{ form.errors.description }}
+            </p>
         </div>
 
         <div class="space-y-2">
-            <label for="category-sort-order" class="text-sm font-medium text-bakery-dark">Sorrend</label>
-            <InputNumber id="category-sort-order" v-model="form.sort_order" :min="0" fluid />
-            <p v-if="form.errors.sort_order" class="text-xs text-red-700">{{ form.errors.sort_order }}</p>
+            <label
+                for="category-sort-order"
+                class="text-sm font-medium text-bakery-dark"
+                >{{ $t("admin_categories.columns.sort_order") }}</label
+            >
+            <InputNumber
+                id="category-sort-order"
+                v-model="form.sort_order"
+                :min="0"
+                fluid
+            />
+            <p v-if="form.errors.sort_order" class="text-xs text-red-700">
+                {{ form.errors.sort_order }}
+            </p>
         </div>
 
         <div class="flex items-center gap-2 pt-7">
             <ToggleSwitch id="category-active" v-model="form.is_active" />
-            <label for="category-active" class="text-sm text-bakery-dark/80">Aktív kategória</label>
+            <label for="category-active" class="text-sm text-bakery-dark/80">{{
+                $t("admin_categories.active_category")
+            }}</label>
         </div>
     </div>
 </template>
-
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Categories/CreateModal.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Categories/CreateModal.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Categories/CreateModal.vue	2026-04-22 12:55:40.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Categories/CreateModal.vue	2026-05-05 07:12:20.108864001 +0000
@@ -23,7 +23,7 @@
     <Dialog
         :visible="visible"
         modal
-        header="Új kategória"
+        :header="$t('admin_categories.actions.create')"
         :style="{ width: '40rem', maxWidth: '95vw' }"
         :content-style="{ maxHeight: '70vh', overflowY: 'auto' }"
         @update:visible="(value) => emit('update:visible', value)"
@@ -38,15 +38,19 @@
 
         <template #footer>
             <div class="flex justify-end gap-2">
-                <Button type="button" severity="secondary" label="Mégse" @click="close" />
+                <Button
+                    type="button"
+                    severity="secondary"
+                    :label="$t('common.cancel')"
+                    @click="close"
+                />
                 <Button
                     type="submit"
                     form="category-create-form"
-                    label="Létrehozás"
+                    :label="$t('admin_production_plans.actions.store')"
                     :loading="form.processing"
                 />
             </div>
         </template>
     </Dialog>
 </template>
-
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Categories/EditModal.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Categories/EditModal.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Categories/EditModal.vue	2026-04-22 12:55:40.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Categories/EditModal.vue	2026-05-05 07:12:20.109753061 +0000
@@ -1,7 +1,7 @@
 <script setup>
-import Button from 'primevue/button';
-import Dialog from 'primevue/dialog';
-import CategoryForm from './CategoryForm.vue';
+import Button from "primevue/button";
+import Dialog from "primevue/dialog";
+import CategoryForm from "./CategoryForm.vue";
 
 defineProps({
     visible: {
@@ -14,16 +14,16 @@
     },
 });
 
-const emit = defineEmits(['update:visible', 'submit']);
+const emit = defineEmits(["update:visible", "submit"]);
 
-const close = () => emit('update:visible', false);
+const close = () => emit("update:visible", false);
 </script>
 
 <template>
     <Dialog
         :visible="visible"
         modal
-        header="Kategoria szerkesztese"
+        :header="$t('admin_categories.actions.edit')"
         :style="{ width: '40rem', maxWidth: '95vw' }"
         :content-style="{ maxHeight: '70vh', overflowY: 'auto' }"
         @update:visible="(value) => emit('update:visible', value)"
@@ -34,10 +34,19 @@
 
         <template #footer>
             <div class="flex justify-end gap-2">
-                <Button type="button" severity="secondary" label="Mégse" @click="close" />
-                <Button type="submit" form="category-edit-form" label="Mentés" :loading="form.processing" />
+                <Button
+                    type="button"
+                    severity="secondary"
+                    :label="$t('common.cancel')"
+                    @click="close"
+                />
+                <Button
+                    type="submit"
+                    form="category-edit-form"
+                    :label="$t('admin_production_plans.actions.save')"
+                    :loading="form.processing"
+                />
             </div>
         </template>
     </Dialog>
 </template>
-
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Ingredients/CreateModal.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Ingredients/CreateModal.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Ingredients/CreateModal.vue	2026-04-22 12:55:40.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Ingredients/CreateModal.vue	2026-05-05 07:12:20.166459751 +0000
@@ -1,7 +1,7 @@
 <script setup>
-import Button from 'primevue/button';
-import Dialog from 'primevue/dialog';
-import IngredientForm from './IngredientForm.vue';
+import Button from "primevue/button";
+import Dialog from "primevue/dialog";
+import IngredientForm from "./IngredientForm.vue";
 
 defineProps({
     visible: {
@@ -18,30 +18,43 @@
     },
 });
 
-const emit = defineEmits(['update:visible', 'submit']);
+const emit = defineEmits(["update:visible", "submit"]);
 
-const close = () => emit('update:visible', false);
+const close = () => emit("update:visible", false);
 </script>
 
 <template>
     <Dialog
         :visible="visible"
         modal
-        header="Uj alapanyag"
+        :header="$t('admin_ingredients.actions.create')"
         :style="{ width: '52rem', maxWidth: '97vw' }"
         :content-style="{ maxHeight: '70vh', overflowY: 'auto' }"
         @update:visible="(value) => emit('update:visible', value)"
     >
-        <form id="ingredient-create-form" class="space-y-4" @submit.prevent="emit('submit')">
+        <form
+            id="ingredient-create-form"
+            class="space-y-4"
+            @submit.prevent="emit('submit')"
+        >
             <IngredientForm :form="form" :units="units" />
         </form>
 
         <template #footer>
             <div class="flex justify-end gap-2">
-                <Button type="button" severity="secondary" label="Mégse" @click="close" />
-                <Button type="submit" form="ingredient-create-form" label="Létrehozás" :loading="form.processing" />
+                <Button
+                    type="button"
+                    severity="secondary"
+                    :label="$t('common.cancel')"
+                    @click="close"
+                />
+                <Button
+                    type="submit"
+                    form="ingredient-create-form"
+                    :label="$t('admin_production_plans.actions.store')"
+                    :loading="form.processing"
+                />
             </div>
         </template>
     </Dialog>
 </template>
-
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Ingredients/EditModal.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Ingredients/EditModal.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Ingredients/EditModal.vue	2026-04-22 12:55:40.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Ingredients/EditModal.vue	2026-05-05 07:12:20.167165907 +0000
@@ -1,7 +1,7 @@
 <script setup>
-import Button from 'primevue/button';
-import Dialog from 'primevue/dialog';
-import IngredientForm from './IngredientForm.vue';
+import Button from "primevue/button";
+import Dialog from "primevue/dialog";
+import IngredientForm from "./IngredientForm.vue";
 
 defineProps({
     visible: {
@@ -18,30 +18,43 @@
     },
 });
 
-const emit = defineEmits(['update:visible', 'submit']);
+const emit = defineEmits(["update:visible", "submit"]);
 
-const close = () => emit('update:visible', false);
+const close = () => emit("update:visible", false);
 </script>
 
 <template>
     <Dialog
         :visible="visible"
         modal
-        header="Alapanyag szerkesztese"
+        :header="$t('admin_ingredients.actions.edit')"
         :style="{ width: '52rem', maxWidth: '97vw' }"
         :content-style="{ maxHeight: '70vh', overflowY: 'auto' }"
         @update:visible="(value) => emit('update:visible', value)"
     >
-        <form id="ingredient-edit-form" class="space-y-4" @submit.prevent="emit('submit')">
+        <form
+            id="ingredient-edit-form"
+            class="space-y-4"
+            @submit.prevent="emit('submit')"
+        >
             <IngredientForm :form="form" :units="units" />
         </form>
 
         <template #footer>
             <div class="flex justify-end gap-2">
-                <Button type="button" severity="secondary" label="Mégse" @click="close" />
-                <Button type="submit" form="ingredient-edit-form" label="Mentés" :loading="form.processing" />
+                <Button
+                    type="button"
+                    severity="secondary"
+                    :label="$t('common.cancel')"
+                    @click="close"
+                />
+                <Button
+                    type="submit"
+                    form="ingredient-edit-form"
+                    :label="$t('admin_production_plans.actions.save')"
+                    :loading="form.processing"
+                />
             </div>
         </template>
     </Dialog>
 </template>
-
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Ingredients/IngredientForm.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Ingredients/IngredientForm.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Ingredients/IngredientForm.vue	2026-04-27 07:39:18.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Ingredients/IngredientForm.vue	2026-05-05 07:12:20.168304543 +0000
@@ -1,11 +1,11 @@
 <script setup>
-import { watch } from 'vue';
-import InputNumber from 'primevue/inputnumber';
-import InputText from 'primevue/inputtext';
-import Select from 'primevue/select';
-import Textarea from 'primevue/textarea';
-import ToggleSwitch from 'primevue/toggleswitch';
-import { slugify } from '../../../Utils/slugify';
+import { watch } from "vue";
+import InputNumber from "primevue/inputnumber";
+import InputText from "primevue/inputtext";
+import Select from "primevue/select";
+import Textarea from "primevue/textarea";
+import ToggleSwitch from "primevue/toggleswitch";
+import { slugify } from "../../../Utils/slugify";
 
 const props = defineProps({
     form: {
@@ -22,33 +22,59 @@
     () => props.form.name,
     (name) => {
         props.form.slug = slugify(name);
-    },
+    }
 );
 </script>
 
 <template>
     <div class="grid gap-4 md:grid-cols-2">
         <div class="space-y-2 md:col-span-2">
-            <label for="ingredient-name" class="text-sm font-medium text-bakery-dark">Név</label>
-            <InputText id="ingredient-name" v-model="form.name" class="w-full" :invalid="Boolean(form.errors.name)" />
-            <p v-if="form.errors.name" class="text-xs text-red-700">{{ form.errors.name }}</p>
+            <label for="ingredient-name" class="text-sm font-medium text-bakery-dark">{{
+                $t("admin_orders.fields.name")
+            }}</label>
+            <InputText
+                id="ingredient-name"
+                v-model="form.name"
+                class="w-full"
+                :invalid="Boolean(form.errors.name)"
+            />
+            <p v-if="form.errors.name" class="text-xs text-red-700">
+                {{ form.errors.name }}
+            </p>
         </div>
 
         <div class="space-y-2">
-            <label for="ingredient-slug" class="text-sm font-medium text-bakery-dark">Slug</label>
+            <label for="ingredient-slug" class="text-sm font-medium text-bakery-dark">{{
+                $t("common.slug")
+            }}</label>
             <InputText id="ingredient-slug" v-model="form.slug" class="w-full" disabled />
-            <p class="text-xs text-bakery-dark/60">Automatikusan generalodik a nev alapjan.</p>
-            <p v-if="form.errors.slug" class="text-xs text-red-700">{{ form.errors.slug }}</p>
+            <p class="text-xs text-bakery-dark/60">
+                {{ $t("common.automatic_generated") }}.
+            </p>
+            <p v-if="form.errors.slug" class="text-xs text-red-700">
+                {{ form.errors.slug }}
+            </p>
         </div>
 
         <div class="space-y-2">
-            <label for="ingredient-sku" class="text-sm font-medium text-bakery-dark">SKU</label>
-            <InputText id="ingredient-sku" v-model="form.sku" class="w-full" :invalid="Boolean(form.errors.sku)" />
-            <p v-if="form.errors.sku" class="text-xs text-red-700">{{ form.errors.sku }}</p>
+            <label for="ingredient-sku" class="text-sm font-medium text-bakery-dark">{{
+                $t("common.sku")
+            }}</label>
+            <InputText
+                id="ingredient-sku"
+                v-model="form.sku"
+                class="w-full"
+                :invalid="Boolean(form.errors.sku)"
+            />
+            <p v-if="form.errors.sku" class="text-xs text-red-700">
+                {{ form.errors.sku }}
+            </p>
         </div>
 
         <div class="space-y-2">
-            <label for="ingredient-unit" class="text-sm font-medium text-bakery-dark">Mertekegyseg</label>
+            <label for="ingredient-unit" class="text-sm font-medium text-bakery-dark">{{
+                $t("admin_ingredients.filters.unit")
+            }}</label>
             <Select
                 id="ingredient-unit"
                 v-model="form.unit"
@@ -57,11 +83,17 @@
                 option-value="value"
                 class="w-full"
             />
-            <p v-if="form.errors.unit" class="text-xs text-red-700">{{ form.errors.unit }}</p>
+            <p v-if="form.errors.unit" class="text-xs text-red-700">
+                {{ form.errors.unit }}
+            </p>
         </div>
 
         <div class="space-y-2">
-            <label for="ingredient-current-stock" class="text-sm font-medium text-bakery-dark">Aktualis keszlet</label>
+            <label
+                for="ingredient-current-stock"
+                class="text-sm font-medium text-bakery-dark"
+                >{{ $t("admin_procurement_intelligence.columns.current_stock") }}</label
+            >
             <InputNumber
                 id="ingredient-current-stock"
                 v-model="form.current_stock"
@@ -71,11 +103,17 @@
                 :max-fraction-digits="3"
                 fluid
             />
-            <p v-if="form.errors.current_stock" class="text-xs text-red-700">{{ form.errors.current_stock }}</p>
+            <p v-if="form.errors.current_stock" class="text-xs text-red-700">
+                {{ form.errors.current_stock }}
+            </p>
         </div>
 
         <div class="space-y-2">
-            <label for="ingredient-estimated-unit-cost" class="text-sm font-medium text-bakery-dark">Becsult egysegkoltseg (Ft)</label>
+            <label
+                for="ingredient-estimated-unit-cost"
+                class="text-sm font-medium text-bakery-dark"
+                >{{ $t("admin_ingredients.columns.estimated_unit_cost") }} (Ft)</label
+            >
             <InputNumber
                 id="ingredient-estimated-unit-cost"
                 v-model="form.estimated_unit_cost"
@@ -85,11 +123,18 @@
                 :max-fraction-digits="4"
                 fluid
             />
-            <p v-if="form.errors.estimated_unit_cost" class="text-xs text-red-700">{{ form.errors.estimated_unit_cost }}</p>
+            <p v-if="form.errors.estimated_unit_cost" class="text-xs text-red-700">
+                {{ form.errors.estimated_unit_cost }}
+            </p>
         </div>
 
         <div class="space-y-2">
-            <label for="ingredient-minimum-stock" class="text-sm font-medium text-bakery-dark">Minimum készlet / utánrendelési szint</label>
+            <label
+                for="ingredient-minimum-stock"
+                class="text-sm font-medium text-bakery-dark"
+                >{{ $t("admin_procurement_intelligence.columns.minimum_stock") }} /
+                {{ $t("admin_procurement_intelligence.reorder_level") }}</label
+            >
             <InputNumber
                 id="ingredient-minimum-stock"
                 v-model="form.minimum_stock"
@@ -99,20 +144,32 @@
                 :max-fraction-digits="3"
                 fluid
             />
-            <p v-if="form.errors.minimum_stock" class="text-xs text-red-700">{{ form.errors.minimum_stock }}</p>
+            <p v-if="form.errors.minimum_stock" class="text-xs text-red-700">
+                {{ form.errors.minimum_stock }}
+            </p>
         </div>
 
         <div class="space-y-2 md:col-span-2">
-            <label for="ingredient-notes" class="text-sm font-medium text-bakery-dark">Megjegyzés</label>
-            <Textarea id="ingredient-notes" v-model="form.notes" rows="4" auto-resize class="w-full" />
-            <p v-if="form.errors.notes" class="text-xs text-red-700">{{ form.errors.notes }}</p>
+            <label for="ingredient-notes" class="text-sm font-medium text-bakery-dark">{{
+                $t("admin_production_plans.form.notes")
+            }}</label>
+            <Textarea
+                id="ingredient-notes"
+                v-model="form.notes"
+                rows="4"
+                auto-resize
+                class="w-full"
+            />
+            <p v-if="form.errors.notes" class="text-xs text-red-700">
+                {{ form.errors.notes }}
+            </p>
         </div>
 
         <div class="flex items-center gap-2 md:col-span-2">
             <ToggleSwitch id="ingredient-active" v-model="form.is_active" />
-            <label for="ingredient-active" class="text-sm text-bakery-dark/80">Aktív alapanyag</label>
+            <label for="ingredient-active" class="text-sm text-bakery-dark/80">{{
+                $t("admin_ingredients.active_ingredient")
+            }}</label>
         </div>
     </div>
 </template>
-
-
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Ingredients/IngredientStatusBadge.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Ingredients/IngredientStatusBadge.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Ingredients/IngredientStatusBadge.vue	2026-04-22 13:06:32.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Ingredients/IngredientStatusBadge.vue	2026-05-05 07:12:20.169569258 +0000
@@ -12,6 +12,6 @@
         class="inline-flex rounded-full px-2.5 py-1 text-xs font-semibold"
         :class="active ? 'bg-green-100 text-green-800' : 'bg-stone-200 text-stone-700'"
     >
-        {{ active ? 'Aktív' : 'Inaktív' }}
+        {{ active ? $t("common.active") : $t("common.inactive") }}
     </span>
-</template>
+</template>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Ingredients/IngredientStockBadge.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Ingredients/IngredientStockBadge.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Ingredients/IngredientStockBadge.vue	2026-04-22 13:00:04.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Ingredients/IngredientStockBadge.vue	2026-05-05 07:16:22.792623818 +0000
@@ -1,5 +1,6 @@
 <script setup>
-import { computed } from 'vue';
+import { computed } from "vue";
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
 
 const props = defineProps({
     currentStock: {
@@ -17,31 +18,26 @@
 });
 
 const isLowStock = computed(() => props.currentStock <= props.minimumStock);
+const { number } = useLocaleFormat();
 
-const formattedCurrent = computed(() =>
-    new Intl.NumberFormat('hu-HU', {
-        minimumFractionDigits: 0,
-        maximumFractionDigits: 3,
-    }).format(props.currentStock),
-);
-
-const formattedMinimum = computed(() =>
-    new Intl.NumberFormat('hu-HU', {
-        minimumFractionDigits: 0,
-        maximumFractionDigits: 3,
-    }).format(props.minimumStock),
-);
+const formattedCurrent = computed(() => number(props.currentStock));
+const formattedMinimum = computed(() => number(props.minimumStock));
 </script>
 
 <template>
     <div class="space-y-1">
         <span
             class="inline-flex rounded-full px-2.5 py-1 text-xs font-semibold"
-            :class="isLowStock ? 'bg-amber-100 text-amber-800' : 'bg-emerald-100 text-emerald-800'"
+            :class="
+                isLowStock
+                    ? 'bg-amber-100 text-amber-800'
+                    : 'bg-emerald-100 text-emerald-800'
+            "
         >
-            {{ isLowStock ? 'Alacsony készlet' : 'Rendben' }}
+            {{ isLowStock ? "Alacsony készlet" : "Rendben" }}
         </span>
-        <p class="text-xs text-bakery-dark/70">{{ formattedCurrent }} {{ unit }} / min {{ formattedMinimum }} {{ unit }}</p>
+        <p class="text-xs text-bakery-dark/70">
+            {{ formattedCurrent }} {{ unit }} / min {{ formattedMinimum }} {{ unit }}
+        </p>
     </div>
-</template>
-
+</template>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/ProcurementIntelligence/IngredientCostTrendTable.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/ProcurementIntelligence/IngredientCostTrendTable.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/ProcurementIntelligence/IngredientCostTrendTable.vue	2026-05-04 13:49:42.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/ProcurementIntelligence/IngredientCostTrendTable.vue	2026-05-05 07:12:20.183148534 +0000
@@ -1,5 +1,8 @@
 <script setup>
-import { trans } from 'laravel-vue-i18n';
+import { trans } from "laravel-vue-i18n";
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
+
+const { currency } = useLocaleFormat();
 
 defineProps({
     rows: {
@@ -12,65 +15,149 @@
     },
 });
 
-const formatCurrency = (value) =>
-    new Intl.NumberFormat(trans('common.locale'), { style: 'currency', currency: trans('common.currency'), maximumFractionDigits: 0 }).format(Number(value ?? 0));
+const formatCurrency = (value) => currency(value);
 </script>
 
 <template>
     <section class="grid gap-4 xl:grid-cols-[minmax(0,1fr)_360px]">
         <div class="ui-card p-4 sm:p-5">
             <div class="flex items-center justify-between gap-3">
-                <h2 class="text-sm font-semibold uppercase tracking-[0.12em] text-bakery-brown/80">
-                    {{ trans('admin_procurement_intelligence.ingredient_cost_trends.title') }}
+                <h2
+                    class="text-sm font-semibold uppercase tracking-[0.12em] text-bakery-brown/80"
+                >
+                    {{
+                        trans(
+                            "admin_procurement_intelligence.ingredient_cost_trends.title"
+                        )
+                    }}
                 </h2>
                 <span class="text-xs text-bakery-dark/60">
-                    {{ trans('admin_procurement_intelligence.counts.points', { count: rows.length }) }}
+                    {{
+                        trans("admin_procurement_intelligence.counts.points", {
+                            count: rows.length,
+                        })
+                    }}
                 </span>
             </div>
             <div v-if="rows.length" class="mt-4 overflow-x-auto">
-                <table class="min-w-[780px] w-full text-sm">
-                    <thead class="border-b border-bakery-brown/15 text-left text-xs uppercase tracking-widest text-bakery-dark/60">
+                <table class="min-w-195 w-full text-sm">
+                    <thead
+                        class="border-b border-bakery-brown/15 text-left text-xs uppercase tracking-widest text-bakery-dark/60"
+                    >
                         <tr>
-                            <th class="px-2 py-2">{{ trans('admin_procurement_intelligence.columns.date') }}</th>
-                            <th class="px-2 py-2">{{ trans('admin_procurement_intelligence.columns.ingredient') }}</th>
-                            <th class="px-2 py-2">{{ trans('admin_procurement_intelligence.columns.supplier') }}</th>
-                            <th class="px-2 py-2 text-right">{{ trans('admin_procurement_intelligence.columns.average_price') }}</th>
-                            <th class="px-2 py-2 text-right">{{ trans('admin_procurement_intelligence.columns.weighted_average') }}</th>
-                            <th class="px-2 py-2 text-right">{{ trans('admin_procurement_intelligence.columns.last_price') }}</th>
-                            <th class="px-2 py-2 text-right">{{ trans('common.quantity') }}</th>
+                            <th class="px-2 py-2">
+                                {{ trans("admin_procurement_intelligence.columns.date") }}
+                            </th>
+                            <th class="px-2 py-2">
+                                {{
+                                    trans(
+                                        "admin_procurement_intelligence.columns.ingredient"
+                                    )
+                                }}
+                            </th>
+                            <th class="px-2 py-2">
+                                {{
+                                    trans(
+                                        "admin_procurement_intelligence.columns.supplier"
+                                    )
+                                }}
+                            </th>
+                            <th class="px-2 py-2 text-right">
+                                {{
+                                    trans(
+                                        "admin_procurement_intelligence.columns.average_price"
+                                    )
+                                }}
+                            </th>
+                            <th class="px-2 py-2 text-right">
+                                {{
+                                    trans(
+                                        "admin_procurement_intelligence.columns.weighted_average"
+                                    )
+                                }}
+                            </th>
+                            <th class="px-2 py-2 text-right">
+                                {{
+                                    trans(
+                                        "admin_procurement_intelligence.columns.last_price"
+                                    )
+                                }}
+                            </th>
+                            <th class="px-2 py-2 text-right">
+                                {{ trans("common.quantity") }}
+                            </th>
                         </tr>
                     </thead>
                     <tbody>
-                        <tr v-for="row in rows" :key="`${row.period_date}-${row.ingredient_id}-${row.supplier_id ?? 'none'}`" class="border-b border-bakery-brown/10">
-                            <td class="px-2 py-3 font-medium text-bakery-dark">{{ row.period_date }}</td>
-                            <td class="px-2 py-3 text-bakery-dark">{{ row.ingredient_name }}</td>
-                            <td class="px-2 py-3 text-bakery-dark/80">{{ row.supplier_name }}</td>
-                            <td class="px-2 py-3 text-right text-bakery-dark">{{ formatCurrency(row.average_unit_cost) }}</td>
-                            <td class="px-2 py-3 text-right font-semibold text-bakery-dark">{{ formatCurrency(row.weighted_average_cost) }}</td>
-                            <td class="px-2 py-3 text-right text-bakery-dark">{{ formatCurrency(row.last_unit_cost) }}</td>
-                            <td class="px-2 py-3 text-right text-bakery-dark">{{ row.purchased_quantity }} {{ row.unit }}</td>
+                        <tr
+                            v-for="row in rows"
+                            :key="`${row.period_date}-${row.ingredient_id}-${
+                                row.supplier_id ?? 'none'
+                            }`"
+                            class="border-b border-bakery-brown/10"
+                        >
+                            <td class="px-2 py-3 font-medium text-bakery-dark">
+                                {{ row.period_date }}
+                            </td>
+                            <td class="px-2 py-3 text-bakery-dark">
+                                {{ row.ingredient_name }}
+                            </td>
+                            <td class="px-2 py-3 text-bakery-dark/80">
+                                {{ row.supplier_name }}
+                            </td>
+                            <td class="px-2 py-3 text-right text-bakery-dark">
+                                {{ formatCurrency(row.average_unit_cost) }}
+                            </td>
+                            <td
+                                class="px-2 py-3 text-right font-semibold text-bakery-dark"
+                            >
+                                {{ formatCurrency(row.weighted_average_cost) }}
+                            </td>
+                            <td class="px-2 py-3 text-right text-bakery-dark">
+                                {{ formatCurrency(row.last_unit_cost) }}
+                            </td>
+                            <td class="px-2 py-3 text-right text-bakery-dark">
+                                {{ row.purchased_quantity }} {{ row.unit }}
+                            </td>
                         </tr>
                     </tbody>
                 </table>
             </div>
-            <div v-else class="mt-4 rounded-lg border border-dashed border-bakery-brown/25 p-6 text-sm text-bakery-dark/65">
-                {{ trans('admin_procurement_intelligence.ingredient_cost_trends.empty') }}
+            <div
+                v-else
+                class="mt-4 rounded-lg border border-dashed border-bakery-brown/25 p-6 text-sm text-bakery-dark/65"
+            >
+                {{ trans("admin_procurement_intelligence.ingredient_cost_trends.empty") }}
             </div>
         </div>
 
         <aside class="ui-card p-4 sm:p-5">
-            <h2 class="text-sm font-semibold uppercase tracking-[0.12em] text-bakery-brown/80">
-                {{ trans('admin_procurement_intelligence.recent_purchases.title') }}
+            <h2
+                class="text-sm font-semibold uppercase tracking-[0.12em] text-bakery-brown/80"
+            >
+                {{ trans("admin_procurement_intelligence.recent_purchases.title") }}
             </h2>
             <div v-if="recentPurchases.length" class="mt-4 space-y-3">
-                <article v-for="row in recentPurchases" :key="`${row.purchase_date}-${row.ingredient_id}-${row.unit_cost}`" class="rounded-lg border border-bakery-brown/15 p-3">
+                <article
+                    v-for="row in recentPurchases"
+                    :key="`${row.purchase_date}-${row.ingredient_id}-${row.unit_cost}`"
+                    class="rounded-lg border border-bakery-brown/15 p-3"
+                >
                     <p class="font-medium text-bakery-dark">{{ row.ingredient_name }}</p>
-                    <p class="mt-1 text-sm text-bakery-dark/70">{{ row.supplier_name }} · {{ row.purchase_date }}</p>
-                    <p class="mt-2 text-sm text-bakery-dark">{{ row.quantity }} {{ row.unit }} · {{ formatCurrency(row.unit_cost) }}</p>
+                    <p class="mt-1 text-sm text-bakery-dark/70">
+                        {{ row.supplier_name }} · {{ row.purchase_date }}
+                    </p>
+                    <p class="mt-2 text-sm text-bakery-dark">
+                        {{ row.quantity }} {{ row.unit }} ·
+                        {{ formatCurrency(row.unit_cost) }}
+                    </p>
                 </article>
             </div>
-            <p v-else class="mt-4 rounded-lg border border-dashed border-bakery-brown/25 p-4 text-sm text-bakery-dark/65">
-                {{ trans('admin_procurement_intelligence.recent_purchases.empty') }}
+            <p
+                v-else
+                class="mt-4 rounded-lg border border-dashed border-bakery-brown/25 p-4 text-sm text-bakery-dark/65"
+            >
+                {{ trans("admin_procurement_intelligence.recent_purchases.empty") }}
             </p>
         </aside>
     </section>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/ProcurementIntelligence/MinimumStockRecommendationsTable.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/ProcurementIntelligence/MinimumStockRecommendationsTable.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/ProcurementIntelligence/MinimumStockRecommendationsTable.vue	2026-05-04 13:49:42.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/ProcurementIntelligence/MinimumStockRecommendationsTable.vue	2026-05-05 07:12:20.183806829 +0000
@@ -1,6 +1,6 @@
 <script setup>
-import { trans } from 'laravel-vue-i18n';
-import ProcurementUrgencyBadge from './ProcurementUrgencyBadge.vue';
+import { trans } from "laravel-vue-i18n";
+import ProcurementUrgencyBadge from "./ProcurementUrgencyBadge.vue";
 
 const props = defineProps({
     rows: {
@@ -13,7 +13,7 @@
     },
 });
 
-const emit = defineEmits(['update:selectedIds']);
+const emit = defineEmits(["update:selectedIds"]);
 
 const toggleRow = (ingredientId) => {
     const selected = new Set(props.selectedIds);
@@ -23,92 +23,214 @@
         selected.add(ingredientId);
     }
 
-    emit('update:selectedIds', Array.from(selected));
+    emit("update:selectedIds", Array.from(selected));
 };
 
 const toggleAll = () => {
     if (props.selectedIds.length === props.rows.length) {
-        emit('update:selectedIds', []);
+        emit("update:selectedIds", []);
         return;
     }
 
-    emit('update:selectedIds', props.rows.map((row) => row.ingredient_id));
+    emit(
+        "update:selectedIds",
+        props.rows.map((row) => row.ingredient_id)
+    );
 };
 
-const sourceLabel = (source) => trans(`admin_procurement_intelligence.supplier_sources.${source || 'none'}`);
+const sourceLabel = (source) =>
+    trans(`admin_procurement_intelligence.supplier_sources.${source || "none"}`);
 
-const dayLabel = (days) => trans('admin_procurement_intelligence.units.days', { count: days });
+const dayLabel = (days) =>
+    trans("admin_procurement_intelligence.units.days", { count: days });
 </script>
 
 <template>
     <section class="ui-card p-4 sm:p-5">
         <div class="flex items-center justify-between gap-3">
-            <h2 class="text-sm font-semibold uppercase tracking-[0.12em] text-bakery-brown/80">
-                {{ trans('admin_procurement_intelligence.minimum_stock.title') }}
+            <h2
+                class="text-sm font-semibold uppercase tracking-[0.12em] text-bakery-brown/80"
+            >
+                {{ trans("admin_procurement_intelligence.minimum_stock.title") }}
             </h2>
             <span class="text-xs text-bakery-dark/60">
-                {{ trans('admin_procurement_intelligence.counts.ingredients', { count: rows.length }) }}
+                {{
+                    trans("admin_procurement_intelligence.counts.ingredients", {
+                        count: rows.length,
+                    })
+                }}
             </span>
         </div>
         <div v-if="rows.length" class="mt-4 overflow-x-auto">
-            <table class="min-w-[1260px] w-full text-sm">
-                <thead class="border-b border-bakery-brown/15 text-left text-xs uppercase tracking-widest text-bakery-dark/60">
+            <table class="min-w-315 w-full text-sm">
+                <thead
+                    class="border-b border-bakery-brown/15 text-left text-xs uppercase tracking-widest text-bakery-dark/60"
+                >
                     <tr>
                         <th class="px-2 py-2">
                             <input
                                 type="checkbox"
                                 class="h-5 w-5 rounded border-bakery-brown/30 text-bakery-brown"
-                                :checked="rows.length > 0 && selectedIds.length === rows.length"
-                                :aria-label="trans('admin_procurement_intelligence.minimum_stock.select_all')"
+                                :checked="
+                                    rows.length > 0 && selectedIds.length === rows.length
+                                "
+                                :aria-label="
+                                    trans(
+                                        'admin_procurement_intelligence.minimum_stock.select_all'
+                                    )
+                                "
                                 @change="toggleAll"
                             />
                         </th>
-                        <th class="px-2 py-2">{{ trans('admin_procurement_intelligence.columns.ingredient') }}</th>
-                        <th class="px-2 py-2 text-right">{{ trans('admin_procurement_intelligence.columns.current_stock') }}</th>
-                        <th class="px-2 py-2 text-right">{{ trans('admin_procurement_intelligence.columns.minimum_stock') }}</th>
-                        <th class="px-2 py-2 text-right">{{ trans('admin_procurement_intelligence.columns.weekly_average_consumption') }}</th>
-                        <th class="px-2 py-2">{{ trans('admin_procurement_intelligence.columns.recommended_supplier') }}</th>
-                        <th class="px-2 py-2 text-right">{{ trans('admin_procurement_intelligence.columns.lead_time') }}</th>
-                        <th class="px-2 py-2 text-right">{{ trans('admin_procurement_intelligence.columns.pack_size') }}</th>
-                        <th class="px-2 py-2 text-right">{{ trans('admin_procurement_intelligence.columns.minimum_order') }}</th>
-                        <th class="px-2 py-2 text-right">{{ trans('admin_procurement_intelligence.columns.stock_days') }}</th>
-                        <th class="px-2 py-2 text-right">{{ trans('admin_procurement_intelligence.columns.suggested_order') }}</th>
-                        <th class="px-2 py-2">{{ trans('admin_procurement_intelligence.columns.urgency') }}</th>
+                        <th class="px-2 py-2">
+                            {{
+                                trans("admin_procurement_intelligence.columns.ingredient")
+                            }}
+                        </th>
+                        <th class="px-2 py-2 text-right">
+                            {{
+                                trans(
+                                    "admin_procurement_intelligence.columns.current_stock"
+                                )
+                            }}
+                        </th>
+                        <th class="px-2 py-2 text-right">
+                            {{
+                                trans(
+                                    "admin_procurement_intelligence.columns.minimum_stock"
+                                )
+                            }}
+                        </th>
+                        <th class="px-2 py-2 text-right">
+                            {{
+                                trans(
+                                    "admin_procurement_intelligence.columns.weekly_average_consumption"
+                                )
+                            }}
+                        </th>
+                        <th class="px-2 py-2">
+                            {{
+                                trans(
+                                    "admin_procurement_intelligence.columns.recommended_supplier"
+                                )
+                            }}
+                        </th>
+                        <th class="px-2 py-2 text-right">
+                            {{
+                                trans("admin_procurement_intelligence.columns.lead_time")
+                            }}
+                        </th>
+                        <th class="px-2 py-2 text-right">
+                            {{
+                                trans("admin_procurement_intelligence.columns.pack_size")
+                            }}
+                        </th>
+                        <th class="px-2 py-2 text-right">
+                            {{
+                                trans(
+                                    "admin_procurement_intelligence.columns.minimum_order"
+                                )
+                            }}
+                        </th>
+                        <th class="px-2 py-2 text-right">
+                            {{
+                                trans("admin_procurement_intelligence.columns.stock_days")
+                            }}
+                        </th>
+                        <th class="px-2 py-2 text-right">
+                            {{
+                                trans(
+                                    "admin_procurement_intelligence.columns.suggested_order"
+                                )
+                            }}
+                        </th>
+                        <th class="px-2 py-2">
+                            {{ trans("admin_procurement_intelligence.columns.urgency") }}
+                        </th>
                     </tr>
                 </thead>
                 <tbody>
-                    <tr v-for="row in rows" :key="row.ingredient_id" class="border-b border-bakery-brown/10">
+                    <tr
+                        v-for="row in rows"
+                        :key="row.ingredient_id"
+                        class="border-b border-bakery-brown/10"
+                    >
                         <td class="px-2 py-3">
                             <input
                                 type="checkbox"
                                 class="h-5 w-5 rounded border-bakery-brown/30 text-bakery-brown"
                                 :checked="selectedIds.includes(row.ingredient_id)"
-                                :aria-label="trans('admin_procurement_intelligence.minimum_stock.select_one', { name: row.ingredient_name })"
+                                :aria-label="
+                                    trans(
+                                        'admin_procurement_intelligence.minimum_stock.select_one',
+                                        { name: row.ingredient_name }
+                                    )
+                                "
                                 @change="toggleRow(row.ingredient_id)"
                             />
                         </td>
-                        <td class="px-2 py-3 font-medium text-bakery-dark">{{ row.ingredient_name }}</td>
-                        <td class="px-2 py-3 text-right text-bakery-dark">{{ row.current_stock }} {{ row.unit }}</td>
-                        <td class="px-2 py-3 text-right text-bakery-dark">{{ row.minimum_stock }} {{ row.unit }}</td>
-                        <td class="px-2 py-3 text-right text-bakery-dark">{{ row.weekly_average_consumption }} {{ row.unit }}</td>
+                        <td class="px-2 py-3 font-medium text-bakery-dark">
+                            {{ row.ingredient_name }}
+                        </td>
+                        <td class="px-2 py-3 text-right text-bakery-dark">
+                            {{ row.current_stock }} {{ row.unit }}
+                        </td>
+                        <td class="px-2 py-3 text-right text-bakery-dark">
+                            {{ row.minimum_stock }} {{ row.unit }}
+                        </td>
+                        <td class="px-2 py-3 text-right text-bakery-dark">
+                            {{ row.weekly_average_consumption }} {{ row.unit }}
+                        </td>
                         <td class="px-2 py-3 text-bakery-dark">
                             <div>
-                                <p>{{ row.recommended_supplier_name || trans('admin_procurement_intelligence.minimum_stock.no_supplier') }}</p>
-                                <p class="text-xs text-bakery-dark/55">{{ sourceLabel(row.supplier_source) }}</p>
+                                <p>
+                                    {{
+                                        row.recommended_supplier_name ||
+                                        trans(
+                                            "admin_procurement_intelligence.minimum_stock.no_supplier"
+                                        )
+                                    }}
+                                </p>
+                                <p class="text-xs text-bakery-dark/55">
+                                    {{ sourceLabel(row.supplier_source) }}
+                                </p>
                             </div>
                         </td>
-                        <td class="px-2 py-3 text-right text-bakery-dark">{{ row.lead_time_days !== null ? dayLabel(row.lead_time_days) : '-' }}</td>
-                        <td class="px-2 py-3 text-right text-bakery-dark">{{ row.pack_size ? `${row.pack_size} ${row.unit}` : '-' }}</td>
-                        <td class="px-2 py-3 text-right text-bakery-dark">{{ row.minimum_order_quantity ? `${row.minimum_order_quantity} ${row.unit}` : '-' }}</td>
-                        <td class="px-2 py-3 text-right text-bakery-dark">{{ row.days_on_hand ?? '-' }}</td>
-                        <td class="px-2 py-3 text-right font-semibold text-bakery-dark">{{ row.suggested_order_quantity }} {{ row.unit }}</td>
-                        <td class="px-2 py-3"><ProcurementUrgencyBadge :value="row.urgency" /></td>
+                        <td class="px-2 py-3 text-right text-bakery-dark">
+                            {{
+                                row.lead_time_days !== null
+                                    ? dayLabel(row.lead_time_days)
+                                    : "-"
+                            }}
+                        </td>
+                        <td class="px-2 py-3 text-right text-bakery-dark">
+                            {{ row.pack_size ? `${row.pack_size} ${row.unit}` : "-" }}
+                        </td>
+                        <td class="px-2 py-3 text-right text-bakery-dark">
+                            {{
+                                row.minimum_order_quantity
+                                    ? `${row.minimum_order_quantity} ${row.unit}`
+                                    : "-"
+                            }}
+                        </td>
+                        <td class="px-2 py-3 text-right text-bakery-dark">
+                            {{ row.days_on_hand ?? "-" }}
+                        </td>
+                        <td class="px-2 py-3 text-right font-semibold text-bakery-dark">
+                            {{ row.suggested_order_quantity }} {{ row.unit }}
+                        </td>
+                        <td class="px-2 py-3">
+                            <ProcurementUrgencyBadge :value="row.urgency" />
+                        </td>
                     </tr>
                 </tbody>
             </table>
         </div>
-        <div v-else class="mt-4 rounded-lg border border-dashed border-bakery-brown/25 p-6 text-sm text-bakery-dark/65">
-            {{ trans('admin_procurement_intelligence.minimum_stock.empty') }}
+        <div
+            v-else
+            class="mt-4 rounded-lg border border-dashed border-bakery-brown/25 p-6 text-sm text-bakery-dark/65"
+        >
+            {{ trans("admin_procurement_intelligence.minimum_stock.empty") }}
         </div>
     </section>
 </template>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/ProcurementIntelligence/SupplierPriceTrendTable.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/ProcurementIntelligence/SupplierPriceTrendTable.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/ProcurementIntelligence/SupplierPriceTrendTable.vue	2026-05-04 13:49:42.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/ProcurementIntelligence/SupplierPriceTrendTable.vue	2026-05-05 07:12:20.186930587 +0000
@@ -1,5 +1,8 @@
 <script setup>
 import { trans } from 'laravel-vue-i18n';
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
+
+const { currency } = useLocaleFormat();
 
 defineProps({
     rows: {
@@ -11,7 +14,7 @@
 const formatCurrency = (value) =>
     value === null || value === undefined
         ? '-'
-        : new Intl.NumberFormat(trans('common.locale'), { style: 'currency', currency: trans('common.currency'), maximumFractionDigits: 0 }).format(Number(value));
+        : currency(value);
 
 const formatPercent = (value) => `${Number(value ?? 0).toFixed(2)}%`;
 </script>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Products/CreateModal.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Products/CreateModal.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Products/CreateModal.vue	2026-04-22 12:55:40.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Products/CreateModal.vue	2026-05-05 07:12:20.191522609 +0000
@@ -48,4 +48,4 @@
         </template>
     </Dialog>
 </template>
-
+
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Products/EditModal.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Products/EditModal.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Products/EditModal.vue	2026-04-22 12:55:40.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Products/EditModal.vue	2026-05-05 07:12:20.192278679 +0000
@@ -48,4 +48,4 @@
         </template>
     </Dialog>
 </template>
-
+
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Products/ProductForm.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Products/ProductForm.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Products/ProductForm.vue	2026-04-22 13:07:18.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Products/ProductForm.vue	2026-05-05 07:12:20.193258972 +0000
@@ -113,5 +113,5 @@
         </div>
     </div>
 </template>
-
-
+
+
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Purchases/PurchaseForm.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Purchases/PurchaseForm.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Purchases/PurchaseForm.vue	2026-04-24 13:20:56.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Purchases/PurchaseForm.vue	2026-05-05 07:12:20.195667509 +0000
@@ -4,6 +4,9 @@
 import InputText from 'primevue/inputtext';
 import Select from 'primevue/select';
 import Textarea from 'primevue/textarea';
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
+
+const { number } = useLocaleFormat();
 
 const props = defineProps({
     form: {
@@ -63,9 +66,7 @@
 );
 
 const formatMoney = (value) =>
-    new Intl.NumberFormat('hu-HU', {
-        maximumFractionDigits: 0,
-    }).format(Number(value || 0));
+    number(value, { maximumFractionDigits: 0 });
 </script>
 
 <template>
@@ -106,7 +107,7 @@
         <div class="space-y-3 rounded-xl border border-bakery-brown/15 bg-[#fcf8f2] p-3">
             <div class="flex items-center justify-between">
                 <h3 class="text-sm font-semibold uppercase tracking-[0.12em] text-bakery-brown/80">Tételek</h3>
-                <Button label="Tétel hozzáadása" icon="pi pi-plus" outlined class="!min-h-11" @click="addItem" />
+                <Button label="Tétel hozzáadása" icon="pi pi-plus" outlined class="min-h-11!" @click="addItem" />
             </div>
 
             <p v-if="form.errors.items" class="text-xs text-red-700">{{ form.errors.items }}</p>
@@ -161,7 +162,7 @@
                         icon="pi pi-trash"
                         severity="danger"
                         text
-                        class="!min-h-11"
+                        class="min-h-11!"
                         :disabled="form.items.length <= 1"
                         @click="removeItem(index)"
                     />
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Recipes/RecipeIngredientList.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Recipes/RecipeIngredientList.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Recipes/RecipeIngredientList.vue	2026-04-27 07:39:18.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Recipes/RecipeIngredientList.vue	2026-05-05 07:12:20.197713831 +0000
@@ -1,5 +1,8 @@
 <script setup>
 import Button from "primevue/button";
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
+
+const { number } = useLocaleFormat();
 
 const props = defineProps({
     items: {
@@ -11,10 +14,7 @@
 const emit = defineEmits(["edit", "delete"]);
 
 const formatQuantity = (value) =>
-    new Intl.NumberFormat("hu-HU", {
-        minimumFractionDigits: 0,
-        maximumFractionDigits: 3,
-    }).format(value);
+    number(value);
 </script>
 
 <template>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Recipes/RecipeStepModal.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Recipes/RecipeStepModal.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Recipes/RecipeStepModal.vue	2026-04-22 13:15:06.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Recipes/RecipeStepModal.vue	2026-05-05 07:12:20.265582144 +0000
@@ -199,6 +199,6 @@
         </template>
     </Dialog>
 </template>
-
-
-
+
+
+
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Recipes/RecipeTable.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Recipes/RecipeTable.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Recipes/RecipeTable.vue	2026-04-22 14:21:10.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Recipes/RecipeTable.vue	2026-05-05 07:12:20.267940473 +0000
@@ -90,11 +90,11 @@
         </Column>
         <Column header="Műveletek" :exportable="false">
             <template #body="{ data }">
-                <Button icon="pi pi-pencil" text rounded class="!h-11 !w-11" aria-label="Recept szerkesztése" @click="emit('open-editor', data)" />
+                <Button icon="pi pi-pencil" text rounded class="h-11! w-11!" aria-label="Recept szerkesztése" @click="emit('open-editor', data)" />
             </template>
         </Column>
         </DataTable>
     </div>
 </template>
-
-
+
+
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Roles/PermissionGroupCard.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Roles/PermissionGroupCard.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/Roles/PermissionGroupCard.vue	2026-05-03 16:35:42.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/Roles/PermissionGroupCard.vue	2026-05-05 07:12:20.270758238 +0000
@@ -1,6 +1,7 @@
 <script setup>
-import Checkbox from 'primevue/checkbox';
-import PermissionBadge from './PermissionBadge.vue';
+import Checkbox from "primevue/checkbox";
+import PermissionBadge from "./PermissionBadge.vue";
+import { Button } from "primevue";
 
 const props = defineProps({
     groupName: {
@@ -21,7 +22,7 @@
     },
 });
 
-const emit = defineEmits(['toggle', 'toggle-group']);
+const emit = defineEmits(["toggle", "toggle-group"]);
 
 const selectedSet = () => new Set(props.selectedPermissions);
 const isAllSelected = () => props.items.every((item) => selectedSet().has(item.name));
@@ -32,18 +33,21 @@
     <div class="rounded-xl border border-bakery-brown/15 bg-white/70 p-4">
         <div class="mb-3 flex items-center justify-between gap-2">
             <div>
-                <p class="text-sm font-semibold text-bakery-dark">{{ groupLabel(groupName) }}</p>
+                <p class="text-sm font-semibold text-bakery-dark">
+                    {{ groupLabel(groupName) }}
+                </p>
                 <p class="text-xs text-bakery-dark/70">{{ items.length }} jogosultság</p>
             </div>
 
-            <button
+            <Button
                 type="button"
-                class="rounded-lg border border-bakery-brown/20 px-2.5 py-1 text-xs font-medium text-bakery-brown hover:bg-bakery-brown/10 disabled:cursor-not-allowed disabled:opacity-50"
+                unstyled
                 :disabled="disabled"
+                class="rounded-lg border border-bakery-brown/20 px-2.5 py-1 text-xs font-medium text-bakery-brown hover:bg-bakery-brown/10 disabled:cursor-not-allowed disabled:opacity-50"
                 @click="emit('toggle-group', { groupName, selectAll: !isAllSelected() })"
             >
-                {{ isAllSelected() ? 'Mindet törli' : 'Mindet kijelöli' }}
-            </button>
+                {{ isAllSelected() ? "Mindet törli" : "Mindet kijelöli" }}
+            </Button>
         </div>
 
         <ul class="space-y-2">
@@ -60,12 +64,22 @@
                         :disabled="disabled"
                         @update:model-value="() => emit('toggle', permission.name)"
                     />
-                    <label :for="`permission-${permission.name}`" class="flex-1 space-y-1">
+                    <label
+                        :for="`permission-${permission.name}`"
+                        class="flex-1 space-y-1"
+                    >
                         <div class="flex items-center gap-2">
-                            <span class="text-sm font-medium text-bakery-dark">{{ permission.label }}</span>
-                            <PermissionBadge :permission="permission.name" :dangerous="permission.dangerous" />
+                            <span class="text-sm font-medium text-bakery-dark">{{
+                                permission.label
+                            }}</span>
+                            <PermissionBadge
+                                :permission="permission.name"
+                                :dangerous="permission.dangerous"
+                            />
                         </div>
-                        <p class="text-xs text-bakery-dark/70">{{ permission.description }}</p>
+                        <p class="text-xs text-bakery-dark/70">
+                            {{ permission.description }}
+                        </p>
                     </label>
                 </div>
             </li>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/WeeklyMenus/CreateModal.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/WeeklyMenus/CreateModal.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/WeeklyMenus/CreateModal.vue	2026-04-22 12:55:40.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/WeeklyMenus/CreateModal.vue	2026-05-05 07:12:20.284967128 +0000
@@ -35,4 +35,4 @@
         </template>
     </Dialog>
 </template>
-
+
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/WeeklyMenus/WeeklyMenuItemForm.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/WeeklyMenus/WeeklyMenuItemForm.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/WeeklyMenus/WeeklyMenuItemForm.vue	2026-04-28 12:59:18.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/WeeklyMenus/WeeklyMenuItemForm.vue	2026-05-05 07:13:36.085495907 +0000
@@ -3,6 +3,9 @@
 import InputText from 'primevue/inputtext';
 import Select from 'primevue/select';
 import ToggleSwitch from 'primevue/toggleswitch';
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
+
+const { number } = useLocaleFormat();
 
 const props = defineProps({
     form: { type: Object, required: true },
@@ -33,7 +36,7 @@
                             </p>
                         </div>
                         <span class="text-xs text-bakery-brown">
-                            {{ new Intl.NumberFormat('hu-HU').format(slotProps.option.price) }} Ft
+                            {{ number(slotProps.option.price) }} Ft
                         </span>
                     </div>
                 </template>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/WeeklyMenus/WeeklyMenuItemsModal.vue /mnt/data/hajnalhej_locale/resources/js/Components/Admin/WeeklyMenus/WeeklyMenuItemsModal.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Admin/WeeklyMenus/WeeklyMenuItemsModal.vue	2026-04-28 12:59:18.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Admin/WeeklyMenus/WeeklyMenuItemsModal.vue	2026-05-05 07:13:36.087196457 +0000
@@ -7,6 +7,9 @@
 import CreateWeeklyMenuItemModal from "./CreateWeeklyMenuItemModal.vue";
 import EditWeeklyMenuItemModal from "./EditWeeklyMenuItemModal.vue";
 import WeeklyMenuStatusBadge from "./WeeklyMenuStatusBadge.vue";
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
+
+const { number } = useLocaleFormat();
 
 const props = defineProps({
     visible: { type: Boolean, required: true },
@@ -83,12 +86,7 @@
                     <Column field="override_price" header="Felülírt ár">
                         <template #body="{ data }">
                             <span v-if="data.override_price !== null"
-                                >{{
-                                    new Intl.NumberFormat("hu-HU").format(
-                                        data.override_price
-                                    )
-                                }}
-                                Ft</span
+                                >{{ number(data.override_price) }} Ft</span
                             >
                             <span v-else class="text-bakery-dark/60">-</span>
                         </template>
@@ -108,7 +106,7 @@
                                     icon="pi pi-pencil"
                                     text
                                     rounded
-                                    class="!h-11 !w-11"
+                                    class="h-11! w-11!"
                                     aria-label="Tétel szerkesztése"
                                     @click="openEdit(data)"
                                 />
@@ -117,7 +115,7 @@
                                     text
                                     rounded
                                     severity="danger"
-                                    class="!h-11 !w-11"
+                                    class="h-11! w-11!"
                                     aria-label="Tétel törlése"
                                     @click="remove(data)"
                                 />
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/AppFooter.vue /mnt/data/hajnalhej_locale/resources/js/Components/AppFooter.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/AppFooter.vue	2026-04-22 15:04:56.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/AppFooter.vue	2026-05-05 07:12:20.009226305 +0000
@@ -1,17 +1,22 @@
 <template>
-    <footer class="mt-20 border-t border-bakery-brown/15 bg-[#efe4d3]/92 backdrop-blur-sm">
+    <footer
+        class="mt-20 border-t border-bakery-brown/15 bg-[#efe4d3]/92 backdrop-blur-sm"
+    >
         <div
             class="mx-auto flex w-full max-w-6xl flex-col gap-4 px-4 py-8 sm:flex-row sm:items-center sm:justify-between sm:px-6"
         >
             <div>
-                <p class="font-heading text-xl tracking-[0.01em] text-bakery-dark">Hajnalhéj Bakery</p>
-                <p class="text-sm text-bakery-dark/75">
-                    Kovászos sütemények és kenyerek Budapesten.
+                <p class="font-heading text-xl tracking-[0.01em] text-bakery-dark">
+                    {{ $t("brand.name") }} {{ $t("bakery") }}
                 </p>
+                <p class="text-sm text-bakery-dark/75">{{ $t("brand.description") }}.</p>
             </div>
             <div class="text-sm text-bakery-brown/90">
-                <p>Kovász. Idő. Türelem.</p>
-                <p>{{ new Date().getFullYear() }} Hajnalhéj. Minden jog fenntartva.</p>
+                <p>{{ $t("home.philosophy") }}</p>
+                <p>
+                    {{ new Date().getFullYear() }} {{ $t("brand.name") }}.
+                    {{ $t("all_right") }}.
+                </p>
             </div>
         </div>
     </footer>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/Public/WeeklyMenuSection.vue /mnt/data/hajnalhej_locale/resources/js/Components/Public/WeeklyMenuSection.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/Public/WeeklyMenuSection.vue	2026-05-04 13:49:42.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/Public/WeeklyMenuSection.vue	2026-05-05 07:12:20.105405160 +0000
@@ -1,6 +1,10 @@
 <script setup>
 import { Link, useForm } from "@inertiajs/vue3";
 import { trans } from "laravel-vue-i18n";
+import { Button } from "primevue";
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
+
+const { currency } = useLocaleFormat();
 
 defineProps({
     menu: {
@@ -30,12 +34,7 @@
     });
 };
 
-const formatCurrency = (value) =>
-    new Intl.NumberFormat(trans("common.locale"), {
-        style: "currency",
-        currency: trans("common.currency"),
-        maximumFractionDigits: 0,
-    }).format(Number(value ?? 0));
+const formatCurrency = (value) => currency(value);
 </script>
 
 <template>
@@ -112,14 +111,15 @@
                         >
                             {{ item.stock_note }}
                         </p>
-                        <button
+                        <Button
                             type="button"
-                            class="mt-4 inline-flex rounded-full bg-bakery-brown px-4 py-2 text-sm font-semibold text-bakery-cream transition hover:bg-bakery-dark disabled:cursor-not-allowed disabled:opacity-70"
+                            unstyled
                             :disabled="cartForm.processing"
+                            class="mt-4 inline-flex rounded-full bg-bakery-brown px-4 py-2 text-sm font-semibold text-bakery-cream transition hover:bg-bakery-dark disabled:cursor-not-allowed disabled:opacity-70"
                             @click="addToCart(item.product_id)"
                         >
                             {{ $t("common.add_to_card") }}
-                        </button>
+                        </Button>
                     </div>
                 </div>
             </article>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Components/SidebarGroup.vue /mnt/data/hajnalhej_locale/resources/js/Components/SidebarGroup.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Components/SidebarGroup.vue	2026-05-03 16:35:42.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Components/SidebarGroup.vue	2026-05-05 07:12:20.015588173 +0000
@@ -1,6 +1,7 @@
 <script setup>
 import { ref } from "vue";
 import SidebarItem from "./SidebarItem.vue";
+import { Button } from "primevue";
 
 const props = defineProps({
     group: {
@@ -19,19 +20,21 @@
 
 <template>
     <section class="mt-5 first:mt-0">
-        <button
+        <Button
             v-if="group.collapsible"
             type="button"
+            unstyled
             class="flex w-full items-center justify-between px-3 text-left text-xs font-semibold uppercase tracking-[0.16em] text-bakery-brown/55"
             @click="toggle"
         >
             <span class="truncate">{{ group.label }}</span>
+
             <i
                 class="pi pi-chevron-down text-[0.65rem] transition"
                 :class="{ '-rotate-90': collapsed }"
                 aria-hidden="true"
             />
-        </button>
+        </Button>
 
         <p
             v-else
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Layouts/AdminLayout.vue /mnt/data/hajnalhej_locale/resources/js/Layouts/AdminLayout.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Layouts/AdminLayout.vue	2026-05-04 13:52:56.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Layouts/AdminLayout.vue	2026-05-05 07:12:20.018248956 +0000
@@ -6,6 +6,7 @@
 import AdminSidebar from "../Components/AdminSidebar.vue";
 import FlashToast from "../Components/FlashToast.vue";
 import { trans } from "laravel-vue-i18n";
+import { Button } from "primevue";
 
 const page = usePage();
 const logoutForm = useForm({});
@@ -239,13 +240,14 @@
                                 page.props.auth?.user?.name
                             }}</span>
                         </p>
-                        <button
+                        <Button
                             type="button"
+                            unstyled
                             class="rounded-full border border-bakery-brown/25 px-4 py-2 text-sm font-semibold text-bakery-brown transition hover:bg-bakery-brown hover:text-bakery-cream focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-bakery-gold/70"
                             @click="logout"
                         >
                             {{ $t("common.logout") }}
-                        </button>
+                        </Button>
                     </div>
                 </template>
             </AppHeader>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/AuditLogs/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/AuditLogs/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/AuditLogs/Index.vue	2026-05-04 14:40:00.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/AuditLogs/Index.vue	2026-05-05 07:12:20.067222639 +0000
@@ -1,6 +1,6 @@
 <script setup>
 import { Head, Link, router } from "@inertiajs/vue3";
-import { computed, reactive, ref } from "vue";
+import { computed, ref } from "vue";
 import Button from "primevue/button";
 import Column from "primevue/column";
 import DataTable from "primevue/datatable";
@@ -12,7 +12,8 @@
 import SectionTitle from "@/Components/SectionTitle.vue";
 import AdminLayout from "@/Layouts/AdminLayout.vue";
 import { trans } from "laravel-vue-i18n";
-import { perPageOptions } from "@/Utils/functions.js";
+import { useAdminFilterState } from "@/composables/useAdminFilterState.js";
+import { pageOptions as createPerPageOptions } from "@/Utils/functions.js";
 
 defineOptions({ layout: AdminLayout });
 
@@ -45,18 +46,38 @@
 
 const loading = ref(false);
 
-const filterState = reactive({
-    search: props.filters.search ?? "",
-    log_name: props.filters.log_name ?? "",
-    event_key: props.filters.event_key ?? "",
-    subject_type: props.filters.subject_type ?? "",
-    per_page: props.filters.per_page ?? 20,
+const {
+    filterState,
+    sortOrder,
+    load,
+    submitFilters,
+    clearFilters,
+    onSort,
+    onPage,
+} = useAdminFilterState({
+    filters: props.filters,
+    defaults: {
+        search: "",
+        log_name: "",
+        event_key: "",
+        subject_type: "",
+        per_page: 20,
+    },
+    routeName: "admin.audit-logs.index",
+    loading,
+    toQuery: (state) => ({
+        search: state.search || undefined,
+        log_name: state.log_name || undefined,
+        event_key: state.event_key || undefined,
+        subject_type: state.subject_type || undefined,
+        per_page: state.per_page,
+    }),
 });
 
 const currentPage = computed(() => props.logs.current_page ?? 1);
 const first = computed(() => (currentPage.value - 1) * (props.logs.per_page ?? 20));
 
-const perPageOptions = perPageOptions(trans, [20, 50, 100]);
+const perPageOptions = createPerPageOptions(trans, [20, 50, 100]);
 /*
 const perPageOptions = [
     { label: trans("common.page_count", { count: 20 }), value: 20 },
@@ -93,45 +114,6 @@
         value: "order",
     },
 ]);
-
-const load = (extra = {}) => {
-    loading.value = true;
-
-    router.get(
-        route("admin.audit-logs.index"),
-        {
-            search: filterState.search || undefined,
-            log_name: filterState.log_name || undefined,
-            event_key: filterState.event_key || undefined,
-            subject_type: filterState.subject_type || undefined,
-            per_page: filterState.per_page,
-            ...extra,
-        },
-        {
-            preserveState: true,
-            preserveScroll: true,
-            replace: true,
-            onFinish: () => {
-                loading.value = false;
-            },
-        }
-    );
-};
-
-const submitFilters = () => load({ page: 1 });
-const clearFilters = () => {
-    filterState.search = "";
-    filterState.log_name = "";
-    filterState.event_key = "";
-    filterState.subject_type = "";
-    filterState.per_page = 20;
-    submitFilters();
-};
-
-const onPage = (event) => {
-    filterState.per_page = event.rows;
-    load({ page: event.page + 1, per_page: event.rows });
-};
 </script>
 
 <template>
@@ -323,7 +305,7 @@
                                     :label="$t('audit_logs.actions.details')"
                                     size="small"
                                     text
-                                    class="!min-h-11"
+                                    class="min-h-11!"
                                 />
                             </Link>
                         </template>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Categories/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Categories/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Categories/Index.vue	2026-05-04 14:50:50.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Categories/Index.vue	2026-05-05 07:12:20.069075766 +0000
@@ -1,6 +1,6 @@
 <script setup>
 import { Head, router, useForm } from "@inertiajs/vue3";
-import { computed, reactive, ref } from "vue";
+import { computed, ref } from "vue";
 import Button from "primevue/button";
 import Column from "primevue/column";
 import ConfirmDialog from "primevue/confirmdialog";
@@ -15,7 +15,8 @@
 import SectionTitle from "@/Components/SectionTitle.vue";
 import AdminLayout from "@/Layouts/AdminLayout.vue";
 import { trans } from "laravel-vue-i18n";
-import { perPageOptions } from "@/Utils/functions.js";
+import { useAdminFilterState } from "@/composables/useAdminFilterState.js";
+import { pageOptions as createPerPageOptions } from "@/Utils/functions.js";
 
 defineOptions({ layout: AdminLayout });
 
@@ -36,21 +37,33 @@
 const editModalVisible = ref(false);
 const editingId = ref(null);
 
-const filterState = reactive({
-    search: props.filters.search ?? "",
-    sort_field: props.filters.sort_field ?? "sort_order",
-    sort_direction: props.filters.sort_direction ?? "asc",
-    per_page: props.filters.per_page ?? 10,
+const {
+    filterState,
+    sortOrder,
+    load,
+    submitFilters,
+    clearFilters,
+    onSort,
+    onPage,
+} = useAdminFilterState({
+    filters: props.filters,
+    defaults: {
+        search: "",
+        sort_field: "sort_order",
+        sort_direction: "asc",
+        per_page: 10,
+    },
+    routeName: "admin.categories.index",
+    loading,
+    toQuery: (state) => ({
+        search: state.search || undefined,
+        sort_field: state.sort_field,
+        sort_direction: state.sort_direction,
+        per_page: state.per_page,
+    }),
 });
 
-const perPageOptions = perPageOptions(trans, [10, 20, 50]);
-/*
-const perPageOptions = [
-    { label: trans("common.page_count", { count: 10 }), value: 10 },
-    { label: trans("common.page_count", { count: 20 }), value: 20 },
-    { label: trans("common.page_count", { count: 50 }), value: 50 },
-];
-*/
+const perPageOptions = createPerPageOptions(trans, [10, 20, 50]);
 
 const form = useForm({
     name: "",
@@ -60,53 +73,9 @@
     sort_order: 0,
 });
 
-const sortOrder = computed(() => (filterState.sort_direction === "asc" ? 1 : -1));
 const currentPage = computed(() => props.categories.current_page ?? 1);
 const first = computed(() => (currentPage.value - 1) * (props.categories.per_page ?? 10));
 
-const load = (extra = {}) => {
-    loading.value = true;
-
-    router.get(
-        route("admin.categories.index"),
-        {
-            search: filterState.search || undefined,
-            sort_field: filterState.sort_field,
-            sort_direction: filterState.sort_direction,
-            per_page: filterState.per_page,
-            ...extra,
-        },
-        {
-            preserveState: true,
-            replace: true,
-            preserveScroll: true,
-            onFinish: () => {
-                loading.value = false;
-            },
-        }
-    );
-};
-
-const submitFilters = () => load({ page: 1 });
-const clearFilters = () => {
-    filterState.search = "";
-    filterState.per_page = 10;
-    filterState.sort_field = "sort_order";
-    filterState.sort_direction = "asc";
-    submitFilters();
-};
-
-const onSort = (event) => {
-    filterState.sort_field = event.sortField;
-    filterState.sort_direction = event.sortOrder === 1 ? "asc" : "desc";
-    load({ page: 1 });
-};
-
-const onPage = (event) => {
-    filterState.per_page = event.rows;
-    load({ page: event.page + 1, per_page: event.rows });
-};
-
 const openCreate = () => {
     editModalVisible.value = false;
     editingId.value = null;
@@ -323,7 +292,7 @@
                                     icon="pi pi-pencil"
                                     text
                                     rounded
-                                    class="!h-11 !w-11"
+                                    class="h-11! w-11!"
                                     :aria-label="$t('admin_categories.actions.edit')"
                                     @click="openEdit(data)"
                                 />
@@ -332,7 +301,7 @@
                                     text
                                     rounded
                                     severity="danger"
-                                    class="!h-11 !w-11"
+                                    class="h-11! w-11!"
                                     :aria-label="$t('admin_categories.actions.delete')"
                                     @click="confirmDelete(data)"
                                 />
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/CeoDashboard/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/CeoDashboard/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/CeoDashboard/Index.vue	2026-05-04 13:49:42.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/CeoDashboard/Index.vue	2026-05-05 07:13:36.166841810 +0000
@@ -1,14 +1,17 @@
 <script setup>
 import { Head, router } from "@inertiajs/vue3";
 import { computed } from "vue";
-import { currentLocale, trans, transChoice } from "laravel-vue-i18n";
+import { trans, transChoice } from "laravel-vue-i18n";
 import Select from "primevue/select";
 import AdminLayout from "@/Layouts/AdminLayout.vue";
 
 import { createDayOptions } from "@/Utils/functions";
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
 
 defineOptions({ layout: AdminLayout });
 
+const { currency, percent, date } = useLocaleFormat();
+
 const props = defineProps({
     filters: {
         type: Object,
@@ -20,15 +23,6 @@
     },
 });
 
-const localeCode = computed(() => currentLocale.value ?? "hu");
-const numberLocale = computed(() => {
-    const locales = {
-        hu: "hu-HU",
-        en: "en-US",
-    };
-
-    return locales[localeCode.value] ?? localeCode.value;
-});
 
 const formatDayOption = (days) => transChoice("common.day_count", days, { count: days });
 
@@ -52,35 +46,13 @@
 const conversion = computed(() => props.dashboard.conversion ?? {});
 const kpiInsights = computed(() => props.dashboard.kpi_insights ?? {});
 
-const formatCurrency = (value) =>
-    new Intl.NumberFormat(numberLocale.value, {
-        style: "currency",
-        currency: "HUF",
-        maximumFractionDigits: 0,
-    }).format(Number(value ?? 0));
-
-const formatPercent = (value) =>
-    new Intl.NumberFormat(numberLocale.value, {
-        style: "percent",
-        minimumFractionDigits: 2,
-        maximumFractionDigits: 2,
-    }).format(Number(value ?? 0) / 100);
-
-const formatSignedPercent = (value) =>
-    new Intl.NumberFormat(numberLocale.value, {
-        style: "percent",
-        signDisplay: "exceptZero",
-        minimumFractionDigits: 2,
-        maximumFractionDigits: 2,
-    }).format(Number(value ?? 0) / 100);
-
-const formatDate = (value) => {
-    if (!value) {
-        return "-";
-    }
+const formatCurrency = (value) => currency(value);
 
-    return new Intl.DateTimeFormat(numberLocale.value).format(new Date(value));
-};
+const formatPercent = (value) => percent(value);
+
+const formatSignedPercent = (value) => percent(value, { signDisplay: "exceptZero" });
+
+const formatDate = (value) => date(value);
 
 const trendIcon = (direction) => {
     if (direction === "up") {
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/ConversionAnalytics/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/ConversionAnalytics/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/ConversionAnalytics/Index.vue	2026-05-04 13:49:42.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/ConversionAnalytics/Index.vue	2026-05-05 07:14:43.767533761 +0000
@@ -1,14 +1,17 @@
 <script setup>
 import { Head, router } from "@inertiajs/vue3";
 import { computed } from "vue";
-import { trans, currentLocale, transChoice } from "laravel-vue-i18n";
+import { trans, transChoice } from "laravel-vue-i18n";
 import Select from "primevue/select";
 import AdminLayout from "@/Layouts/AdminLayout.vue";
 
 import { createDayOptions } from "@/Utils/functions";
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
 
 defineOptions({ layout: AdminLayout });
 
+const { currency, percent, number, date } = useLocaleFormat();
+
 const props = defineProps({
     filters: {
         type: Object,
@@ -44,37 +47,12 @@
 const funnelStats = computed(() => props.analytics.funnel_stats ?? []);
 const heroComparison = computed(() => props.analytics.hero_comparison ?? []);
 const dropOffRows = computed(() => props.analytics.drop_off_top ?? []);
-const localeCode = computed(() => currentLocale.value ?? "hu");
-const numberLocale = computed(() => {
-    const locales = {
-        hu: "hu-HU",
-        en: "en-US",
-    };
 
-    return locales[localeCode.value] ?? localeCode.value;
-});
+const formatPercent = (value) => percent(value);
 
-const formatPercent = (value) =>
-    new Intl.NumberFormat(numberLocale.value, {
-        style: "percent",
-        minimumFractionDigits: 2,
-        maximumFractionDigits: 2,
-    }).format(Number(value ?? 0) / 100);
-
-const formatCurrency = (value) =>
-    new Intl.NumberFormat(numberLocale.value, {
-        style: "currency",
-        currency: "HUF",
-        maximumFractionDigits: 0,
-    }).format(Number(value ?? 0));
-
-const formatDate = (value) => {
-    if (!value) {
-        return "-";
-    }
+const formatCurrency = (value) => currency(value);
 
-    return new Intl.DateTimeFormat(numberLocale.value).format(new Date(value));
-};
+const formatDate = (value) => date(value);
 </script>
 
 <template>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/IngredientSupplierTerms/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/IngredientSupplierTerms/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/IngredientSupplierTerms/Index.vue	2026-05-04 14:38:20.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/IngredientSupplierTerms/Index.vue	2026-05-05 07:12:20.074378422 +0000
@@ -1,6 +1,6 @@
 <script setup>
 import { Head, router, useForm } from "@inertiajs/vue3";
-import { computed, reactive, ref } from "vue";
+import { computed, ref } from "vue";
 import Button from "primevue/button";
 import Column from "primevue/column";
 import ConfirmDialog from "primevue/confirmdialog";
@@ -15,10 +15,14 @@
 import SectionTitle from "@/Components/SectionTitle.vue";
 import AdminLayout from "@/Layouts/AdminLayout.vue";
 import { trans } from "laravel-vue-i18n";
-import { perPageOptions } from "@/Utils/functions.js";
+import { useAdminFilterState } from "@/composables/useAdminFilterState.js";
+import { pageOptions as createPerPageOptions } from "@/Utils/functions.js";
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
 
 defineOptions({ layout: AdminLayout });
 
+const { number, currency } = useLocaleFormat();
+
 const props = defineProps({
     terms: { type: Object, required: true },
     filters: { type: Object, required: true },
@@ -32,15 +36,35 @@
 const editModalVisible = ref(false);
 const editingId = ref(null);
 
-const filterState = reactive({
-    search: props.filters.search ?? "",
-    active: props.filters.active ?? "",
-    sort_field: props.filters.sort_field ?? "ingredient",
-    sort_direction: props.filters.sort_direction ?? "asc",
-    per_page: props.filters.per_page ?? 10,
+const {
+    filterState,
+    sortOrder,
+    load,
+    submitFilters,
+    clearFilters,
+    onSort,
+    onPage,
+} = useAdminFilterState({
+    filters: props.filters,
+    defaults: {
+        search: "",
+        active: "",
+        sort_field: "ingredient",
+        sort_direction: "asc",
+        per_page: 10,
+    },
+    routeName: "admin.ingredient-supplier-terms.index",
+    loading,
+    toQuery: (state) => ({
+        search: state.search || undefined,
+        active: state.active,
+        sort_field: state.sort_field,
+        sort_direction: state.sort_direction,
+        per_page: state.per_page,
+    }),
 });
 
-const perPageOptions = perPageOptions(trans, [10, 20, 50]);
+const perPageOptions = createPerPageOptions(trans, [10, 20, 50]);
 /*
 const perPageOptions = [
     { label: trans("common.page_count", { count: 10 }), value: 10 },
@@ -67,7 +91,6 @@
     meta: "",
 });
 
-const sortOrder = computed(() => (filterState.sort_direction === "asc" ? 1 : -1));
 const currentPage = computed(() => props.terms.current_page ?? 1);
 const first = computed(() => (currentPage.value - 1) * (props.terms.per_page ?? 10));
 
@@ -92,51 +115,6 @@
     return JSON.stringify(meta, null, 2);
 };
 
-const load = (extra = {}) => {
-    loading.value = true;
-
-    router.get(
-        route("admin.ingredient-supplier-terms.index"),
-        {
-            search: filterState.search || undefined,
-            active: filterState.active,
-            sort_field: filterState.sort_field,
-            sort_direction: filterState.sort_direction,
-            per_page: filterState.per_page,
-            ...extra,
-        },
-        {
-            preserveState: true,
-            replace: true,
-            preserveScroll: true,
-            onFinish: () => {
-                loading.value = false;
-            },
-        }
-    );
-};
-
-const submitFilters = () => load({ page: 1 });
-const clearFilters = () => {
-    filterState.search = "";
-    filterState.active = "";
-    filterState.sort_field = "ingredient";
-    filterState.sort_direction = "asc";
-    filterState.per_page = 10;
-    submitFilters();
-};
-
-const onSort = (event) => {
-    filterState.sort_field = event.sortField;
-    filterState.sort_direction = event.sortOrder === 1 ? "asc" : "desc";
-    load({ page: 1 });
-};
-
-const onPage = (event) => {
-    filterState.per_page = event.rows;
-    load({ page: event.page + 1, per_page: event.rows });
-};
-
 const openCreate = () => {
     editModalVisible.value = false;
     editingId.value = null;
@@ -210,9 +188,7 @@
         return "-";
     }
 
-    return `${Number(value).toLocaleString(trans("common.locale"), {
-        maximumFractionDigits: 3,
-    })}${unit ? ` ${unit}` : ""}`;
+    return `${number(value, { maximumFractionDigits: 3 })}${unit ? ` ${unit}` : ""}`;
 };
 
 const formatCurrency = (value) => {
@@ -220,11 +196,7 @@
         return "-";
     }
 
-    return new Intl.NumberFormat(trans("common.locale"), {
-        style: "currency",
-        currency: trans("common.currency"),
-        maximumFractionDigits: 0,
-    }).format(Number(value));
+    return currency(value);
 };
 </script>
 
@@ -457,7 +429,7 @@
                                     icon="pi pi-pencil"
                                     text
                                     rounded
-                                    class="!h-11 !w-11"
+                                    class="h-11! w-11!"
                                     :aria-label="$t('admin_supplier_terms.actions.edit')"
                                     @click="openEdit(data)"
                                 />
@@ -466,7 +438,7 @@
                                     text
                                     rounded
                                     severity="danger"
-                                    class="!h-11 !w-11"
+                                    class="h-11! w-11!"
                                     :aria-label="
                                         $t('admin_supplier_terms.actions.delete')
                                     "
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Ingredients/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Ingredients/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Ingredients/Index.vue	2026-05-04 14:39:26.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Ingredients/Index.vue	2026-05-05 07:12:48.870712208 +0000
@@ -1,6 +1,6 @@
 <script setup>
 import { Head, router, useForm } from "@inertiajs/vue3";
-import { computed, reactive, ref, transformVNodeArgs } from "vue";
+import { computed, ref, transformVNodeArgs } from "vue";
 import Button from "primevue/button";
 import Column from "primevue/column";
 import ConfirmDialog from "primevue/confirmdialog";
@@ -16,10 +16,14 @@
 import SectionTitle from "../../../Components/SectionTitle.vue";
 import AdminLayout from "../../../Layouts/AdminLayout.vue";
 import { trans } from "laravel-vue-i18n";
-import { perPageOptions } from "@/Utils/functions.js";
+import { useAdminFilterState } from "@/composables/useAdminFilterState.js";
+import { pageOptions as createPerPageOptions } from "@/Utils/functions.js";
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
 
 defineOptions({ layout: AdminLayout });
 
+const { currency } = useLocaleFormat();
+
 const props = defineProps({
     ingredients: {
         type: Object,
@@ -41,16 +45,37 @@
 const editModalVisible = ref(false);
 const editingId = ref(null);
 
-const filterState = reactive({
-    search: props.filters.search ?? "",
-    is_active: props.filters.is_active ?? "",
-    unit: props.filters.unit ?? "",
-    sort_field: props.filters.sort_field ?? "name",
-    sort_direction: props.filters.sort_direction ?? "asc",
-    per_page: props.filters.per_page ?? 10,
+const {
+    filterState,
+    sortOrder,
+    load,
+    submitFilters,
+    clearFilters,
+    onSort,
+    onPage,
+} = useAdminFilterState({
+    filters: props.filters,
+    defaults: {
+        search: "",
+        is_active: "",
+        unit: "",
+        sort_field: "name",
+        sort_direction: "asc",
+        per_page: 10,
+    },
+    routeName: "admin.ingredients.index",
+    loading,
+    toQuery: (state) => ({
+        search: state.search || undefined,
+        is_active: state.is_active,
+        unit: state.unit || undefined,
+        sort_field: state.sort_field,
+        sort_direction: state.sort_direction,
+        per_page: state.per_page,
+    }),
 });
 
-const perPageOptions = perPageOptions(trans, [10, 20, 50]);
+const perPageOptions = createPerPageOptions(trans, [10, 20, 50]);
 /*
 const perPageOptions = [
     { label: trans("common.page_count", { count: 10 }), value: 10 },
@@ -75,11 +100,7 @@
         return "-";
     }
 
-    return new Intl.NumberFormat(trans("common.locale"), {
-        style: "currency",
-        currency: trans("common.currency"),
-        maximumFractionDigits: 0,
-    }).format(Number(value));
+    return currency(value);
 };
 
 const form = useForm({
@@ -94,59 +115,11 @@
     notes: "",
 });
 
-const sortOrder = computed(() => (filterState.sort_direction === "asc" ? 1 : -1));
 const currentPage = computed(() => props.ingredients.current_page ?? 1);
 const first = computed(
     () => (currentPage.value - 1) * (props.ingredients.per_page ?? 10)
 );
 
-const load = (extra = {}) => {
-    loading.value = true;
-
-    router.get(
-        route("admin.ingredients.index"),
-        {
-            search: filterState.search || undefined,
-            is_active: filterState.is_active,
-            unit: filterState.unit || undefined,
-            sort_field: filterState.sort_field,
-            sort_direction: filterState.sort_direction,
-            per_page: filterState.per_page,
-            ...extra,
-        },
-        {
-            preserveState: true,
-            replace: true,
-            preserveScroll: true,
-            onFinish: () => {
-                loading.value = false;
-            },
-        }
-    );
-};
-
-const submitFilters = () => load({ page: 1 });
-const clearFilters = () => {
-    filterState.search = "";
-    filterState.is_active = "";
-    filterState.unit = "";
-    filterState.sort_field = "name";
-    filterState.sort_direction = "asc";
-    filterState.per_page = 10;
-    submitFilters();
-};
-
-const onSort = (event) => {
-    filterState.sort_field = event.sortField;
-    filterState.sort_direction = event.sortOrder === 1 ? "asc" : "desc";
-    load({ page: 1 });
-};
-
-const onPage = (event) => {
-    filterState.per_page = event.rows;
-    load({ page: event.page + 1, per_page: event.rows });
-};
-
 const openCreate = () => {
     editModalVisible.value = false;
     editingId.value = null;
@@ -425,7 +398,7 @@
                                     icon="pi pi-pencil"
                                     text
                                     rounded
-                                    class="!h-11 !w-11"
+                                    class="h-11! w-11!"
                                     :aria-label="$t('admin_ingredients.actions.edit')"
                                     @click="openEdit(data)"
                                 />
@@ -434,7 +407,7 @@
                                     text
                                     rounded
                                     severity="danger"
-                                    class="!h-11 !w-11"
+                                    class="h-11! w-11!"
                                     :aria-label="$t('admin_ingredients.actions.delete')"
                                     @click="confirmDelete(data)"
                                 />
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Inventory/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Inventory/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Inventory/Index.vue	2026-05-04 14:35:36.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Inventory/Index.vue	2026-05-05 07:12:20.075566301 +0000
@@ -1,6 +1,6 @@
 <script setup>
 import { Head, router, useForm } from "@inertiajs/vue3";
-import { computed, reactive, ref } from "vue";
+import { computed, ref } from "vue";
 import Button from "primevue/button";
 import Column from "primevue/column";
 import DataTable from "primevue/datatable";
@@ -14,11 +14,15 @@
 import SectionTitle from "@/Components/SectionTitle.vue";
 import AdminLayout from "@/Layouts/AdminLayout.vue";
 import { trans } from "laravel-vue-i18n";
+import { useAdminFilterState } from "@/composables/useAdminFilterState.js";
 
-import { createDayOptions, perPageOptions } from "@/Utils/functions";
+import { createDayOptions, pageOptions as createPerPageOptions } from "@/Utils/functions";
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
 
 defineOptions({ layout: AdminLayout });
 
+const { currency } = useLocaleFormat();
+
 const props = defineProps({
     dashboard: { type: Object, required: true },
     ledger: { type: Object, required: true },
@@ -33,20 +37,41 @@
 const wasteModalVisible = ref(false);
 const adjustmentModalVisible = ref(false);
 
-const filterState = reactive({
-    days: props.filters.days ?? 7,
-    date_from: props.filters.date_from ?? "",
-    date_to: props.filters.date_to ?? "",
-    search: props.filters.search ?? "",
-    movement_type: props.filters.movement_type ?? "",
-    ingredient_id: props.filters.ingredient_id || null,
-    per_page: props.filters.per_page ?? 15,
+const {
+    filterState,
+    sortOrder,
+    load,
+    submitFilters,
+    clearFilters,
+    onSort,
+    onPage,
+} = useAdminFilterState({
+    filters: props.filters,
+    defaults: {
+    days: 7,
+    date_from: "",
+    date_to: "",
+    search: "",
+    movement_type: "",
+    ingredient_id: null,
+    per_page: 15,
+},
+    routeName: "admin.inventory.index",
+    loading,
+    toQuery: (state) => ({
+            days: state.days,
+            date_from: state.date_from || undefined,
+            date_to: state.date_to || undefined,
+            search: state.search || undefined,
+            movement_type: state.movement_type || undefined,
+            ingredient_id: state.ingredient_id || undefined,
+            per_page: state.per_page,
+        }),
 });
 
 const dayOptions = createDayOptions(trans, [7, 14, 30, 90]);
 
-import { trans } from "laravel-vue-i18n";
-const perPageOptions = perPageOptions(trans, [15, 30, 50]);
+const perPageOptions = createPerPageOptions(trans, [15, 30, 50]);
 /*
 const perPageOptions = [
     { label: trans("common.page_count", { count: 15 }), value: 15 },
@@ -107,49 +132,9 @@
     () => (currentPage.value - 1) * (props.ledger.per_page ?? filterState.per_page)
 );
 
-const load = (extra = {}) => {
-    loading.value = true;
-
-    router.get(
-        route("admin.inventory.index"),
-        {
-            days: filterState.days,
-            date_from: filterState.date_from || undefined,
-            date_to: filterState.date_to || undefined,
-            search: filterState.search || undefined,
-            movement_type: filterState.movement_type || undefined,
-            ingredient_id: filterState.ingredient_id || undefined,
-            per_page: filterState.per_page,
-            ...extra,
-        },
-        {
-            preserveState: true,
-            replace: true,
-            preserveScroll: true,
-            onFinish: () => {
-                loading.value = false;
-            },
-        }
-    );
-};
 
-const submitFilters = () => load({ page: 1 });
 
-const clearFilters = () => {
-    filterState.days = 7;
-    filterState.date_from = "";
-    filterState.date_to = "";
-    filterState.search = "";
-    filterState.movement_type = "";
-    filterState.ingredient_id = null;
-    filterState.per_page = 15;
-    submitFilters();
-};
 
-const onPage = (event) => {
-    filterState.per_page = event.rows;
-    load({ page: event.page + 1, per_page: event.rows });
-};
 
 const openWasteModal = () => {
     wasteForm.reset();
@@ -194,12 +179,7 @@
     });
 };
 
-const asCurrency = (value) =>
-    new Intl.NumberFormat(trans("common.locale"), {
-        style: "currency",
-        currency: trans("common.currency"),
-        maximumFractionDigits: 0,
-    }).format(Number(value ?? 0));
+const asCurrency = (value) => currency(value);
 
 const movementTypeLabel = (type) => {
     const map = {
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Orders/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Orders/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Orders/Index.vue	2026-05-04 14:37:44.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Orders/Index.vue	2026-05-05 07:12:20.076781150 +0000
@@ -1,6 +1,6 @@
 <script setup>
 import { Head, Link, router } from "@inertiajs/vue3";
-import { computed, reactive, ref } from "vue";
+import { computed, ref } from "vue";
 import Column from "primevue/column";
 import DataTable from "primevue/datatable";
 import InputText from "primevue/inputtext";
@@ -12,10 +12,14 @@
 import SectionTitle from "@/Components/SectionTitle.vue";
 import AdminLayout from "@/Layouts/AdminLayout.vue";
 import { trans } from "laravel-vue-i18n";
-import { perPageOptions } from "@/Utils/functions.js";
+import { useAdminFilterState } from "@/composables/useAdminFilterState.js";
+import { pageOptions as createPerPageOptions } from "@/Utils/functions.js";
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
 
 defineOptions({ layout: AdminLayout });
 
+const { currency } = useLocaleFormat();
+
 const props = defineProps({
     orders: {
         type: Object,
@@ -33,19 +37,38 @@
 
 const loading = ref(false);
 
-const filterState = reactive({
-    search: props.filters.search ?? "",
-    status: props.filters.status ?? "",
-    sort_field: props.filters.sort_field ?? "placed_at",
-    sort_direction: props.filters.sort_direction ?? "desc",
-    per_page: props.filters.per_page ?? 15,
+const {
+    filterState,
+    sortOrder,
+    load,
+    submitFilters,
+    clearFilters,
+    onSort,
+    onPage,
+} = useAdminFilterState({
+    filters: props.filters,
+    defaults: {
+    search: "",
+    status: "",
+    sort_field: "placed_at",
+    sort_direction: "desc",
+    per_page: 15,
+},
+    routeName: "admin.orders.index",
+    loading,
+    toQuery: (state) => ({
+            search: state.search || undefined,
+            status: state.status || undefined,
+            sort_field: state.sort_field,
+            sort_direction: state.sort_direction,
+            per_page: state.per_page,
+        }),
 });
 
 const currentPage = computed(() => props.orders.current_page ?? 1);
 const first = computed(() => (currentPage.value - 1) * (props.orders.per_page ?? 15));
-const sortOrder = computed(() => (filterState.sort_direction === "asc" ? 1 : -1));
 
-const perPageOptions = perPageOptions(trans, [10, 20, 50]);
+const perPageOptions = createPerPageOptions(trans, [10, 20, 50]);
 
 /*
 const perPageOptions = [
@@ -60,57 +83,11 @@
     ...props.statusOptions.map((status) => ({ label: status, value: status })),
 ]);
 
-const formatCurrency = (value) =>
-    new Intl.NumberFormat(trans("common.locale"), {
-        style: "currency",
-        currency: trans("common.currency"),
-        maximumFractionDigits: 0,
-    }).format(Number(value ?? 0));
-
-const load = (extra = {}) => {
-    loading.value = true;
-
-    router.get(
-        route("admin.orders.index"),
-        {
-            search: filterState.search || undefined,
-            status: filterState.status || undefined,
-            sort_field: filterState.sort_field,
-            sort_direction: filterState.sort_direction,
-            per_page: filterState.per_page,
-            ...extra,
-        },
-        {
-            preserveState: true,
-            preserveScroll: true,
-            replace: true,
-            onFinish: () => {
-                loading.value = false;
-            },
-        }
-    );
-};
-
-const submitFilters = () => load({ page: 1 });
-const clearFilters = () => {
-    filterState.search = "";
-    filterState.status = "";
-    filterState.sort_field = "placed_at";
-    filterState.sort_direction = "desc";
-    filterState.per_page = 15;
-    submitFilters();
-};
-
-const onSort = (event) => {
-    filterState.sort_field = event.sortField;
-    filterState.sort_direction = event.sortOrder === 1 ? "asc" : "desc";
-    load({ page: 1 });
-};
-
-const onPage = (event) => {
-    filterState.per_page = event.rows;
-    load({ page: event.page + 1, per_page: event.rows });
-};
+const formatCurrency = (value) => currency(value);
+
+
+
+
 </script>
 
 <template>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Orders/Show.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Orders/Show.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Orders/Show.vue	2026-05-04 13:49:42.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Orders/Show.vue	2026-05-05 07:12:20.077984749 +0000
@@ -9,9 +9,12 @@
 import SectionTitle from "@/Components/SectionTitle.vue";
 import AdminLayout from "@/Layouts/AdminLayout.vue";
 import { trans } from "laravel-vue-i18n";
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
 
 defineOptions({ layout: AdminLayout });
 
+const { currency } = useLocaleFormat();
+
 const props = defineProps({
     order: {
         type: Object,
@@ -30,12 +33,7 @@
     pickup_time_slot: props.order.pickup_time_slot ?? "",
 });
 
-const formatCurrency = (value) =>
-    new Intl.NumberFormat(trans("common.locale"), {
-        style: "currency",
-        currency: trans("common.currency"),
-        maximumFractionDigits: 0,
-    }).format(Number(value ?? 0));
+const formatCurrency = (value) => currency(value);
 
 const updateStatus = () => {
     statusForm.patch(route("admin.orders.status.update", props.order.id));
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Permissions/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Permissions/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Permissions/Index.vue	2026-05-04 14:34:20.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Permissions/Index.vue	2026-05-05 07:12:20.079066632 +0000
@@ -1,7 +1,9 @@
 <script setup>
 import { Head, Link, router, usePage } from "@inertiajs/vue3";
-import { computed, reactive, ref } from "vue";
+import { computed, ref } from "vue";
 import { trans } from "laravel-vue-i18n";
+import { useAdminFilterState } from "@/composables/useAdminFilterState.js";
+import { pageOptions as createPerPageOptions } from "@/Utils/functions.js";
 import Button from "primevue/button";
 import Checkbox from "primevue/checkbox";
 import Column from "primevue/column";
@@ -43,15 +45,38 @@
 const syncing = ref(false);
 const syncSummaryVisible = ref(Boolean(page.props.flash?.sync_summary));
 
-const filterState = reactive({
-    search: props.filters.search ?? "",
-    module: props.filters.module ?? "",
-    dangerous_only: props.filters.dangerous_only ?? false,
-    usage_state: props.filters.usage_state ?? "",
-    registry_state: props.filters.registry_state ?? "",
-    sort_field: props.filters.sort_field ?? "name",
-    sort_direction: props.filters.sort_direction ?? "asc",
-    per_page: props.filters.per_page ?? 20,
+const {
+    filterState,
+    sortOrder,
+    load,
+    submitFilters,
+    clearFilters,
+    onSort,
+    onPage,
+} = useAdminFilterState({
+    filters: props.filters,
+    defaults: {
+        search: "",
+        module: "",
+        dangerous_only: false,
+        usage_state: "",
+        registry_state: "",
+        sort_field: "name",
+        sort_direction: "asc",
+        per_page: 20,
+    },
+    routeName: "admin.permissions.index",
+    loading,
+    toQuery: (state) => ({
+        search: state.search || undefined,
+        module: state.module || undefined,
+        dangerous_only: state.dangerous_only ? 1 : undefined,
+        usage_state: state.usage_state || undefined,
+        registry_state: state.registry_state || undefined,
+        sort_field: state.sort_field,
+        sort_direction: state.sort_direction,
+        per_page: state.per_page,
+    }),
 });
 
 const currentPage = computed(() => props.permissions.current_page ?? 1);
@@ -103,9 +128,7 @@
     { label: trans("admin_permissions.sort_directions.desc"), value: "desc" },
 ];
 
-import { perPageOptions } from "@/Utils/functions.js";
-import { trans } from "laravel-vue-i18n";
-const perPageOptions = perPageOptions(trans, [20, 50, 100]);
+const perPageOptions = createPerPageOptions(trans, [20, 50, 100]);
 /*
 const perPageOptions = [
     { label: trans("common.page_count", { count: 20 }), value: 20 },
@@ -114,40 +137,6 @@
 ];
 */
 
-const load = (extra = {}) => {
-    loading.value = true;
-
-    router.get(
-        route("admin.permissions.index"),
-        {
-            search: filterState.search || undefined,
-            module: filterState.module || undefined,
-            dangerous_only: filterState.dangerous_only ? 1 : undefined,
-            usage_state: filterState.usage_state || undefined,
-            registry_state: filterState.registry_state || undefined,
-            sort_field: filterState.sort_field,
-            sort_direction: filterState.sort_direction,
-            per_page: filterState.per_page,
-            ...extra,
-        },
-        {
-            preserveState: true,
-            preserveScroll: true,
-            replace: true,
-            onFinish: () => {
-                loading.value = false;
-            },
-        }
-    );
-};
-
-const submitFilters = () => load({ page: 1 });
-
-const onPage = (event) => {
-    filterState.per_page = event.rows;
-    load({ page: event.page + 1, per_page: event.rows });
-};
-
 const runSync = () => {
     syncing.value = true;
     router.post(
@@ -162,17 +151,6 @@
         }
     );
 };
-const clearFilters = () => {
-    filterState.search = "";
-    filterState.module = "";
-    filterState.dangerous_only = false;
-    filterState.usage_state = "";
-    filterState.registry_state = "";
-    filterState.sort_field = "name";
-    filterState.sort_direction = "asc";
-    filterState.per_page = 20;
-    submitFilters();
-};
 </script>
 
 <template>
@@ -432,7 +410,7 @@
                                     :label="$t('admin_permissions.actions.details')"
                                     size="small"
                                     text
-                                    class="!min-h-11"
+                                    class="min-h-11!"
                                 />
                             </Link>
                         </template>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/ProcurementIntelligence/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/ProcurementIntelligence/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/ProcurementIntelligence/Index.vue	2026-05-04 13:49:42.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/ProcurementIntelligence/Index.vue	2026-05-05 07:12:20.080909924 +0000
@@ -118,7 +118,7 @@
                     </p>
                 </div>
 
-                <div class="grid gap-3 sm:grid-cols-2 xl:w-[680px] xl:grid-cols-5">
+                <div class="grid gap-3 sm:grid-cols-2 xl:w-170 xl:grid-cols-5">
                     <Select
                         :model-value="filters.days"
                         :options="filter_options.days"
@@ -165,13 +165,14 @@
                         class="min-h-11 w-full"
                         @update:model-value="updateFilter('urgency', $event)"
                     />
-                    <button
+                    <Button
                         type="button"
+                        unstyled
                         class="min-h-11 rounded-lg border border-bakery-brown/25 px-4 text-sm font-semibold text-bakery-brown transition hover:bg-bakery-brown/10"
                         @click="resetFilters"
                     >
                         {{ trans("common.clear_filters") }}
-                    </button>
+                    </Button>
                 </div>
             </div>
 
@@ -201,7 +202,7 @@
                                 'admin_procurement_intelligence.actions.generate_purchase_drafts'
                             )
                         "
-                        class="!min-h-11"
+                        class="min-h-11!"
                         :disabled="generatableCount === 0 || draftGenerationProcessing"
                         :loading="draftGenerationProcessing"
                         @click="generatePurchaseDrafts"
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/ProductionPlans/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/ProductionPlans/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/ProductionPlans/Index.vue	2026-05-04 14:38:46.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/ProductionPlans/Index.vue	2026-05-05 07:12:20.082047211 +0000
@@ -1,6 +1,6 @@
 <script setup>
 import { Head, router, useForm } from "@inertiajs/vue3";
-import { computed, reactive, ref } from "vue";
+import { computed, ref } from "vue";
 import Button from "primevue/button";
 import Column from "primevue/column";
 import ConfirmDialog from "primevue/confirmdialog";
@@ -9,12 +9,13 @@
 import Select from "primevue/select";
 import { useConfirm } from "primevue/useconfirm";
 import { trans } from "laravel-vue-i18n";
+import { useAdminFilterState } from "@/composables/useAdminFilterState.js";
 import AdminTableToolbar from "@/Components/Admin/AdminTableToolbar.vue";
 import CreateModal from "@/Components/Admin/ProductionPlans/CreateModal.vue";
 import EditModal from "@/Components/Admin/ProductionPlans/EditModal.vue";
 import SectionTitle from "@/Components/SectionTitle.vue";
 import AdminLayout from "@/Layouts/AdminLayout.vue";
-import { perPageOptions } from "@/Utils/functions.js";
+import { pageOptions as createPerPageOptions } from "@/Utils/functions.js";
 
 defineOptions({ layout: AdminLayout });
 
@@ -32,14 +33,36 @@
 const editModalVisible = ref(false);
 const editingId = ref(null);
 
-const filterState = reactive({
-    search: props.filters.search ?? "",
-    status: props.filters.status ?? "",
-    target_from: props.filters.target_from ?? "",
-    target_to: props.filters.target_to ?? "",
-    sort_field: props.filters.sort_field ?? "target_at",
-    sort_direction: props.filters.sort_direction ?? "asc",
-    per_page: props.filters.per_page ?? 10,
+const {
+    filterState,
+    sortOrder,
+    load,
+    submitFilters,
+    clearFilters,
+    onSort,
+    onPage,
+} = useAdminFilterState({
+    filters: props.filters,
+    defaults: {
+        search: "",
+        status: "",
+        target_from: "",
+        target_to: "",
+        sort_field: "target_at",
+        sort_direction: "asc",
+        per_page: 10,
+    },
+    routeName: "admin.production-plans.index",
+    loading,
+    toQuery: (state) => ({
+        search: state.search || undefined,
+        status: state.status || undefined,
+        target_from: state.target_from || undefined,
+        target_to: state.target_to || undefined,
+        sort_field: state.sort_field,
+        sort_direction: state.sort_direction,
+        per_page: state.per_page,
+    }),
 });
 
 const statusOptions = computed(() => [
@@ -47,7 +70,7 @@
     ...props.statuses,
 ]);
 
-const perPageOptions = perPageOptions(trans, [10, 20, 50]);
+const perPageOptions = createPerPageOptions(trans, [10, 20, 50]);
 /*
 const perPageOptions = computed(() =>
     [10, 20, 50].map((count) => ({
@@ -68,7 +91,6 @@
 const selectedPlan = computed(
     () => props.productionPlans.data.find((plan) => plan.id === editingId.value) ?? null
 );
-const sortOrder = computed(() => (filterState.sort_direction === "asc" ? 1 : -1));
 const currentPage = computed(() => props.productionPlans.current_page ?? 1);
 const first = computed(
     () => (currentPage.value - 1) * (props.productionPlans.per_page ?? 10)
@@ -113,55 +135,6 @@
     form.items = [makeDefaultItem()];
 };
 
-const load = (extra = {}) => {
-    loading.value = true;
-
-    router.get(
-        route("admin.production-plans.index"),
-        {
-            search: filterState.search || undefined,
-            status: filterState.status || undefined,
-            target_from: filterState.target_from || undefined,
-            target_to: filterState.target_to || undefined,
-            sort_field: filterState.sort_field,
-            sort_direction: filterState.sort_direction,
-            per_page: filterState.per_page,
-            ...extra,
-        },
-        {
-            preserveState: true,
-            replace: true,
-            preserveScroll: true,
-            onFinish: () => {
-                loading.value = false;
-            },
-        }
-    );
-};
-
-const submitFilters = () => load({ page: 1 });
-const clearFilters = () => {
-    filterState.search = "";
-    filterState.status = "";
-    filterState.target_from = "";
-    filterState.target_to = "";
-    filterState.sort_field = "target_at";
-    filterState.sort_direction = "asc";
-    filterState.per_page = 10;
-    submitFilters();
-};
-
-const onSort = (event) => {
-    filterState.sort_field = event.sortField;
-    filterState.sort_direction = event.sortOrder === 1 ? "asc" : "desc";
-    load({ page: 1 });
-};
-
-const onPage = (event) => {
-    filterState.per_page = event.rows;
-    load({ page: event.page + 1, per_page: event.rows });
-};
-
 const openCreate = () => {
     editingId.value = null;
     editModalVisible.value = false;
@@ -457,7 +430,7 @@
                                     icon="pi pi-pencil"
                                     text
                                     rounded
-                                    class="!h-11 !w-11"
+                                    class="h-11! w-11!"
                                     :aria-label="
                                         trans('admin_production_plans.actions.edit')
                                     "
@@ -468,7 +441,7 @@
                                     text
                                     rounded
                                     severity="danger"
-                                    class="!h-11 !w-11"
+                                    class="h-11! w-11!"
                                     :aria-label="
                                         trans('admin_production_plans.actions.delete')
                                     "
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Products/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Products/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Products/Index.vue	2026-05-04 14:43:06.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Products/Index.vue	2026-05-05 07:12:20.083322195 +0000
@@ -1,6 +1,6 @@
 <script setup>
 import { Head, Link, router, useForm } from "@inertiajs/vue3";
-import { computed, reactive, ref } from "vue";
+import { computed, ref } from "vue";
 import Button from "primevue/button";
 import Column from "primevue/column";
 import ConfirmDialog from "primevue/confirmdialog";
@@ -16,8 +16,9 @@
 import ProductPrice from "@/Components/Admin/Products/ProductPrice.vue";
 import SectionTitle from "@/Components/SectionTitle.vue";
 import AdminLayout from "@/Layouts/AdminLayout.vue";
-import { perPageOptions } from "@/Utils/functions.js";
+import { pageOptions as createPerPageOptions } from "@/Utils/functions.js";
 import { trans } from "laravel-vue-i18n";
+import { useAdminFilterState } from "@/composables/useAdminFilterState.js";
 
 defineOptions({ layout: AdminLayout });
 
@@ -46,16 +47,37 @@
 const editModalVisible = ref(false);
 const editingId = ref(null);
 
-const filterState = reactive({
-    search: props.filters.search ?? "",
-    category_id: props.filters.category_id ?? null,
-    is_active: props.filters.is_active ?? "",
-    sort_field: props.filters.sort_field ?? "sort_order",
-    sort_direction: props.filters.sort_direction ?? "asc",
-    per_page: props.filters.per_page ?? 10,
+const {
+    filterState,
+    sortOrder,
+    load,
+    submitFilters,
+    clearFilters,
+    onSort,
+    onPage,
+} = useAdminFilterState({
+    filters: props.filters,
+    defaults: {
+    search: "",
+    category_id: null,
+    is_active: "",
+    sort_field: "sort_order",
+    sort_direction: "asc",
+    per_page: 10,
+},
+    routeName: "admin.products.index",
+    loading,
+    toQuery: (state) => ({
+            search: state.search || undefined,
+            category_id: state.category_id || undefined,
+            is_active: state.is_active,
+            sort_field: state.sort_field,
+            sort_direction: state.sort_direction,
+            per_page: state.per_page,
+        }),
 });
 
-const perPageOptions = perPageOptions(trans, [10, 20, 50]);
+const perPageOptions = createPerPageOptions(trans, [10, 20, 50]);
 /*
 const perPageOptions = [
     { label: "10 / oldal", value: 10 },
@@ -83,56 +105,12 @@
     sort_order: 0,
 });
 
-const sortOrder = computed(() => (filterState.sort_direction === "asc" ? 1 : -1));
 const currentPage = computed(() => props.products.current_page ?? 1);
 const first = computed(() => (currentPage.value - 1) * (props.products.per_page ?? 10));
 
-const load = (extra = {}) => {
-    loading.value = true;
 
-    router.get(
-        route("admin.products.index"),
-        {
-            search: filterState.search || undefined,
-            category_id: filterState.category_id || undefined,
-            is_active: filterState.is_active,
-            sort_field: filterState.sort_field,
-            sort_direction: filterState.sort_direction,
-            per_page: filterState.per_page,
-            ...extra,
-        },
-        {
-            preserveState: true,
-            replace: true,
-            preserveScroll: true,
-            onFinish: () => {
-                loading.value = false;
-            },
-        }
-    );
-};
 
-const submitFilters = () => load({ page: 1 });
-const clearFilters = () => {
-    filterState.search = "";
-    filterState.category_id = null;
-    filterState.is_active = "";
-    filterState.sort_field = "sort_order";
-    filterState.sort_direction = "asc";
-    filterState.per_page = 10;
-    submitFilters();
-};
 
-const onSort = (event) => {
-    filterState.sort_field = event.sortField;
-    filterState.sort_direction = event.sortOrder === 1 ? "asc" : "desc";
-    load({ page: 1 });
-};
-
-const onPage = (event) => {
-    filterState.per_page = event.rows;
-    load({ page: event.page + 1, per_page: event.rows });
-};
 
 const openCreate = () => {
     editModalVisible.value = false;
@@ -384,7 +362,7 @@
                                     icon="pi pi-pencil"
                                     text
                                     rounded
-                                    class="!h-11 !w-11"
+                                    class="h-11! w-11!"
                                     aria-label="Termék szerkesztése"
                                     @click="openEdit(data)"
                                 />
@@ -393,7 +371,7 @@
                                     text
                                     rounded
                                     severity="danger"
-                                    class="!h-11 !w-11"
+                                    class="h-11! w-11!"
                                     aria-label="Termék törlése"
                                     @click="confirmDelete(data)"
                                 />
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/ProfitDashboard/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/ProfitDashboard/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/ProfitDashboard/Index.vue	2026-05-04 13:49:42.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/ProfitDashboard/Index.vue	2026-05-05 07:12:20.084672398 +0000
@@ -6,9 +6,12 @@
 import { trans } from "laravel-vue-i18n";
 
 import { createDayOptions } from "@/Utils/functions";
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
 
 defineOptions({ layout: AdminLayout });
 
+const { currency } = useLocaleFormat();
+
 const props = defineProps({
     filters: {
         type: Object,
@@ -38,12 +41,7 @@
 const topProfitProducts = computed(() => props.dashboard.top_profit_products ?? []);
 const trendPoints = computed(() => props.dashboard.order_profit_trend?.points ?? []);
 
-const formatCurrency = (value) =>
-    new Intl.NumberFormat(trans("common.locale"), {
-        style: "currency",
-        currency: trans("common.currency"),
-        maximumFractionDigits: 0,
-    }).format(Number(value ?? 0));
+const formatCurrency = (value) => currency(value);
 
 const formatPercent = (value) => `${Number(value ?? 0).toFixed(2)}%`;
 </script>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Purchases/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Purchases/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Purchases/Index.vue	2026-05-04 14:44:12.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Purchases/Index.vue	2026-05-05 07:12:20.086153147 +0000
@@ -1,6 +1,6 @@
 <script setup>
 import { Head, Link, router, useForm } from "@inertiajs/vue3";
-import { computed, reactive, ref } from "vue";
+import { computed, ref } from "vue";
 import Button from "primevue/button";
 import Column from "primevue/column";
 import ConfirmDialog from "primevue/confirmdialog";
@@ -13,11 +13,15 @@
 import CreateModal from "@/Components/Admin/Purchases/CreateModal.vue";
 import SectionTitle from "@/Components/SectionTitle.vue";
 import AdminLayout from "@/Layouts/AdminLayout.vue";
-import { perPageOptions } from "@/Utils/functions.js";
+import { pageOptions as createPerPageOptions } from "@/Utils/functions.js";
 import { trans } from "laravel-vue-i18n";
+import { useAdminFilterState } from "@/composables/useAdminFilterState.js";
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
 
 defineOptions({ layout: AdminLayout });
 
+const { currency } = useLocaleFormat();
+
 const props = defineProps({
     purchases: {
         type: Object,
@@ -45,16 +49,37 @@
 const loading = ref(false);
 const createModalVisible = ref(false);
 
-const filterState = reactive({
-    search: props.filters.search ?? "",
-    status: props.filters.status ?? "",
-    supplier_id: props.filters.supplier_id || null,
-    sort_field: props.filters.sort_field ?? "purchase_date",
-    sort_direction: props.filters.sort_direction ?? "desc",
-    per_page: props.filters.per_page ?? 10,
+const {
+    filterState,
+    sortOrder,
+    load,
+    submitFilters,
+    clearFilters,
+    onSort,
+    onPage,
+} = useAdminFilterState({
+    filters: props.filters,
+    defaults: {
+    search: "",
+    status: "",
+    supplier_id: null,
+    sort_field: "purchase_date",
+    sort_direction: "desc",
+    per_page: 10,
+},
+    routeName: "admin.purchases.index",
+    loading,
+    toQuery: (state) => ({
+            search: state.search || undefined,
+            status: state.status || undefined,
+            supplier_id: state.supplier_id || undefined,
+            sort_field: state.sort_field,
+            sort_direction: state.sort_direction,
+            per_page: state.per_page,
+        }),
 });
 
-const perPageOptions = perPageOptions(trans, [10, 20, 50]);
+const perPageOptions = createPerPageOptions(trans, [10, 20, 50]);
 /*
 const perPageOptions = [
     { label: '10 / oldal', value: 10 },
@@ -90,56 +115,12 @@
     items: [newItem()],
 });
 
-const sortOrder = computed(() => (filterState.sort_direction === "asc" ? 1 : -1));
 const currentPage = computed(() => props.purchases.current_page ?? 1);
 const first = computed(() => (currentPage.value - 1) * (props.purchases.per_page ?? 10));
 
-const load = (extra = {}) => {
-    loading.value = true;
-
-    router.get(
-        route("admin.purchases.index"),
-        {
-            search: filterState.search || undefined,
-            status: filterState.status || undefined,
-            supplier_id: filterState.supplier_id || undefined,
-            sort_field: filterState.sort_field,
-            sort_direction: filterState.sort_direction,
-            per_page: filterState.per_page,
-            ...extra,
-        },
-        {
-            preserveState: true,
-            replace: true,
-            preserveScroll: true,
-            onFinish: () => {
-                loading.value = false;
-            },
-        }
-    );
-};
 
-const submitFilters = () => load({ page: 1 });
-const clearFilters = () => {
-    filterState.search = "";
-    filterState.status = "";
-    filterState.supplier_id = null;
-    filterState.sort_field = "purchase_date";
-    filterState.sort_direction = "desc";
-    filterState.per_page = 10;
-    submitFilters();
-};
 
-const onSort = (event) => {
-    filterState.sort_field = event.sortField;
-    filterState.sort_direction = event.sortOrder === 1 ? "asc" : "desc";
-    load({ page: 1 });
-};
 
-const onPage = (event) => {
-    filterState.per_page = event.rows;
-    load({ page: event.page + 1, per_page: event.rows });
-};
 
 const openCreate = () => {
     form.reset();
@@ -203,10 +184,7 @@
     });
 };
 
-const formatCurrency = (value) =>
-    `${new Intl.NumberFormat("hu-HU", {
-        maximumFractionDigits: 0,
-    }).format(Number(value || 0))} Ft`;
+const formatCurrency = (value) => currency(value);
 
 const statusLabel = (status) => {
     const map = {
@@ -406,7 +384,7 @@
                                     icon="pi pi-check"
                                     text
                                     rounded
-                                    class="!h-11 !w-11"
+                                    class="h-11! w-11!"
                                     aria-label="Beszerzés könyvelése"
                                     @click="postNow(data)"
                                 />
@@ -416,7 +394,7 @@
                                     text
                                     rounded
                                     severity="danger"
-                                    class="!h-11 !w-11"
+                                    class="h-11! w-11!"
                                     aria-label="Beszerzés stornózása"
                                     @click="cancelPurchase(data)"
                                 />
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Purchases/Show.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Purchases/Show.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Purchases/Show.vue	2026-05-04 13:49:42.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Purchases/Show.vue	2026-05-05 07:13:36.088287777 +0000
@@ -4,9 +4,12 @@
 import Button from 'primevue/button';
 import PurchaseForm from '@/Components/Admin/Purchases/PurchaseForm.vue';
 import AdminLayout from '@/Layouts/AdminLayout.vue';
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
 
 defineOptions({ layout: AdminLayout });
 
+const { number } = useLocaleFormat();
+
 const props = defineProps({
     purchase: { type: Object, required: true },
     suppliers: { type: Array, required: true },
@@ -56,7 +59,7 @@
                 <p><strong>Dátum:</strong> {{ purchase.purchase_date }}</p>
                 <p><strong>Státusz:</strong> {{ purchase.status }}</p>
                 <p><strong>Könyvelt:</strong> {{ purchase.posted_at || '-' }}</p>
-                <p><strong>Összesen:</strong> {{ new Intl.NumberFormat('hu-HU').format(purchase.total) }} Ft</p>
+                <p><strong>Összesen:</strong> {{ number(purchase.total) }} Ft</p>
             </div>
             <p v-if="purchase.notes" class="mt-3 text-sm text-bakery-dark/75">{{ purchase.notes }}</p>
         </div>
@@ -70,7 +73,7 @@
                 <Button
                     label="Tervezet mentése"
                     icon="pi pi-save"
-                    class="!min-h-11"
+                    class="min-h-11!"
                     :loading="form.processing"
                     @click="submitUpdate"
                 />
@@ -92,8 +95,8 @@
                     <tr v-for="item in purchase.items" :key="item.id" class="border-b border-bakery-brown/10">
                         <td class="px-2 py-2">{{ item.ingredient_name }}</td>
                         <td class="px-2 py-2 text-right">{{ item.quantity }} {{ item.unit }}</td>
-                        <td class="px-2 py-2 text-right">{{ new Intl.NumberFormat('hu-HU').format(item.unit_cost) }} Ft</td>
-                        <td class="px-2 py-2 text-right">{{ new Intl.NumberFormat('hu-HU').format(item.line_total) }} Ft</td>
+                        <td class="px-2 py-2 text-right">{{ number(item.unit_cost) }} Ft</td>
+                        <td class="px-2 py-2 text-right">{{ number(item.line_total) }} Ft</td>
                     </tr>
                 </tbody>
             </table>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Recipes/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Recipes/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Recipes/Index.vue	2026-05-04 14:41:46.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Recipes/Index.vue	2026-05-05 07:12:20.088277320 +0000
@@ -14,7 +14,7 @@
 import RecipeTable from "@/Components/Admin/Recipes/RecipeTable.vue";
 import SectionTitle from "@/Components/SectionTitle.vue";
 import AdminLayout from "@/Layouts/AdminLayout.vue";
-import { perPageOptions } from "@/Utils/functions.js";
+import { pageOptions } from "@/Utils/functions.js";
 import { trans } from "laravel-vue-i18n";
 
 defineOptions({ layout: AdminLayout });
@@ -69,7 +69,7 @@
     per_page: props.filters.per_page ?? 10,
 });
 
-const perPageOptions = perPageOptions(trans, [10, 20, 50]);
+const perPageOptions = pageOptions(trans, [10, 20, 50]);
 /*
 const perPageOptions = [
     { label: "10 / oldal", value: 10 },
@@ -439,7 +439,7 @@
                         :options="perPageOptions"
                         option-label="label"
                         option-value="value"
-                        class="w-[9rem]"
+                        class="w-36"
                         @change="submitFilters"
                     />
                     <Button icon="pi pi-search" label="Keresés" @click="submitFilters" />
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Roles/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Roles/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Roles/Index.vue	2026-05-04 14:50:00.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Roles/Index.vue	2026-05-05 07:12:20.089616460 +0000
@@ -1,6 +1,6 @@
 <script setup>
 import { Head, Link, router, useForm } from "@inertiajs/vue3";
-import { computed, reactive, ref } from "vue";
+import { computed, ref } from "vue";
 import Button from "primevue/button";
 import Column from "primevue/column";
 import ConfirmDialog from "primevue/confirmdialog";
@@ -14,8 +14,9 @@
 import RoleFormModal from "@/Components/Admin/Roles/RoleFormModal.vue";
 import SectionTitle from "@/Components/SectionTitle.vue";
 import AdminLayout from "@/Layouts/AdminLayout.vue";
-import { perPageOptions } from "@/Utils/functions";
+import { pageOptions as createPerPageOptions } from "@/Utils/functions";
 import { trans } from "laravel-vue-i18n";
+import { useAdminFilterState } from "@/composables/useAdminFilterState.js";
 
 defineOptions({ layout: AdminLayout });
 
@@ -43,12 +44,29 @@
 const createForm = useForm({ name: "" });
 const editForm = useForm({ name: "" });
 
-const filterState = reactive({
-    search: props.filters.search ?? "",
-    per_page: props.filters.per_page ?? 15,
+const {
+    filterState,
+    sortOrder,
+    load,
+    submitFilters,
+    clearFilters,
+    onSort,
+    onPage,
+} = useAdminFilterState({
+    filters: props.filters,
+    defaults: {
+        search: "",
+        per_page: 15,
+    },
+    routeName: "admin.roles.index",
+    loading,
+    toQuery: (state) => ({
+        search: state.search || undefined,
+        per_page: state.per_page,
+    }),
 });
 
-const perPageOptions = perPageOptions(trans, [15, 30, 50]);
+const perPageOptions = createPerPageOptions(trans, [15, 30, 50]);
 /*
 const perPageOptions = [
     { label: '15 / oldal', value: 15 },
@@ -60,39 +78,6 @@
 const currentPage = computed(() => props.roles.current_page ?? 1);
 const first = computed(() => (currentPage.value - 1) * (props.roles.per_page ?? 15));
 
-const load = (extra = {}) => {
-    loading.value = true;
-
-    router.get(
-        route("admin.roles.index"),
-        {
-            search: filterState.search || undefined,
-            per_page: filterState.per_page,
-            ...extra,
-        },
-        {
-            preserveState: true,
-            preserveScroll: true,
-            replace: true,
-            onFinish: () => {
-                loading.value = false;
-            },
-        }
-    );
-};
-
-const submitFilters = () => load({ page: 1 });
-const clearFilters = () => {
-    filterState.search = "";
-    filterState.per_page = 15;
-    submitFilters();
-};
-
-const onPage = (event) => {
-    filterState.per_page = event.rows;
-    load({ page: event.page + 1, per_page: event.rows });
-};
-
 const openCreate = () => {
     createForm.reset();
     createForm.clearErrors();
@@ -255,7 +240,7 @@
                                         label="Részletek"
                                         size="small"
                                         text
-                                        class="!min-h-11"
+                                        class="min-h-11!"
                                     />
                                 </Link>
                                 <Button
@@ -263,7 +248,7 @@
                                     label="Átnevezés"
                                     size="small"
                                     outlined
-                                    class="!min-h-11"
+                                    class="min-h-11!"
                                     :disabled="data.is_system_role"
                                     @click="openEdit(data)"
                                 />
@@ -273,7 +258,7 @@
                                     size="small"
                                     severity="danger"
                                     text
-                                    class="!min-h-11"
+                                    class="min-h-11!"
                                     :disabled="data.is_system_role"
                                     @click="destroyRole(data)"
                                 />
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/StockCounts/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/StockCounts/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/StockCounts/Index.vue	2026-05-04 13:49:42.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/StockCounts/Index.vue	2026-05-05 07:12:20.093383932 +0000
@@ -45,7 +45,7 @@
             <div class="grid gap-3 md:grid-cols-3">
                 <Select v-model="status" :options="[{ label: 'Mind', value: '' }, ...statuses.map((s) => ({ label: s, value: s }))]" option-label="label" option-value="value" />
                 <div />
-                <Button label="Szűrés" class="!min-h-11" @click="load" />
+                <Button label="Szűrés" class="min-h-11!" @click="load" />
             </div>
 
             <div class="overflow-x-auto">
@@ -83,7 +83,7 @@
                     <InputText v-model="item.counted_quantity" type="number" step="0.001" placeholder="Számolt mennyiség" />
                 </div>
             </div>
-            <Button label="Leltár mentése" class="!min-h-11" :disabled="form.processing" @click="form.post(route('admin.stock-counts.store'), { preserveScroll: true })" />
+            <Button label="Leltár mentése" class="min-h-11!" :disabled="form.processing" @click="form.post(route('admin.stock-counts.store'), { preserveScroll: true })" />
         </section>
     </div>
 </template>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/StockCounts/Show.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/StockCounts/Show.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/StockCounts/Show.vue	2026-05-04 13:49:42.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/StockCounts/Show.vue	2026-05-05 07:12:20.094387980 +0000
@@ -27,7 +27,7 @@
                 <p><strong>Lezárva:</strong> {{ stock_count.closed_at || '-' }}</p>
             </div>
             <p v-if="stock_count.notes" class="mt-3 text-sm text-bakery-dark/75">{{ stock_count.notes }}</p>
-            <Button v-if="stock_count.status === 'draft'" label="Leltár lezárása és könyvelés" class="!min-h-11 mt-4" @click="closeCount" />
+            <Button v-if="stock_count.status === 'draft'" label="Leltár lezárása és könyvelés" class="min-h-11! mt-4" @click="closeCount" />
         </div>
 
         <div class="ui-card p-4 sm:p-5 overflow-x-auto">
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Suppliers/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Suppliers/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Suppliers/Index.vue	2026-05-04 14:47:00.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Suppliers/Index.vue	2026-05-05 07:12:20.095144405 +0000
@@ -1,6 +1,6 @@
 <script setup>
 import { Head, router, useForm } from "@inertiajs/vue3";
-import { computed, reactive, ref } from "vue";
+import { computed, ref } from "vue";
 import Button from "primevue/button";
 import Column from "primevue/column";
 import ConfirmDialog from "primevue/confirmdialog";
@@ -13,11 +13,15 @@
 import EditModal from "@/Components/Admin/Suppliers/EditModal.vue";
 import SectionTitle from "@/Components/SectionTitle.vue";
 import AdminLayout from "@/Layouts/AdminLayout.vue";
-import { perPageOptions } from "@/Utils/functions";
+import { pageOptions as createPerPageOptions } from "@/Utils/functions";
 import { trans } from "laravel-vue-i18n";
+import { useAdminFilterState } from "@/composables/useAdminFilterState.js";
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
 
 defineOptions({ layout: AdminLayout });
 
+const { dateTime } = useLocaleFormat();
+
 const props = defineProps({
     suppliers: {
         type: Object,
@@ -35,14 +39,33 @@
 const editModalVisible = ref(false);
 const editingId = ref(null);
 
-const filterState = reactive({
-    search: props.filters.search ?? "",
-    sort_field: props.filters.sort_field ?? "name",
-    sort_direction: props.filters.sort_direction ?? "asc",
-    per_page: props.filters.per_page ?? 10,
+const {
+    filterState,
+    sortOrder,
+    load,
+    submitFilters,
+    clearFilters,
+    onSort,
+    onPage,
+} = useAdminFilterState({
+    filters: props.filters,
+    defaults: {
+    search: "",
+    sort_field: "name",
+    sort_direction: "asc",
+    per_page: 10,
+},
+    routeName: "admin.suppliers.index",
+    loading,
+    toQuery: (state) => ({
+            search: state.search || undefined,
+            sort_field: state.sort_field,
+            sort_direction: state.sort_direction,
+            per_page: state.per_page,
+        }),
 });
 
-const perPageOptions = perPageOptions(trans, [15, 30, 50]);
+const perPageOptions = createPerPageOptions(trans, [15, 30, 50]);
 /*
 const perPageOptions = [
     { label: '10 / oldal', value: 10 },
@@ -60,52 +83,12 @@
     notes: "",
 });
 
-const sortOrder = computed(() => (filterState.sort_direction === "asc" ? 1 : -1));
 const currentPage = computed(() => props.suppliers.current_page ?? 1);
 const first = computed(() => (currentPage.value - 1) * (props.suppliers.per_page ?? 10));
 
-const load = (extra = {}) => {
-    loading.value = true;
 
-    router.get(
-        route("admin.suppliers.index"),
-        {
-            search: filterState.search || undefined,
-            sort_field: filterState.sort_field,
-            sort_direction: filterState.sort_direction,
-            per_page: filterState.per_page,
-            ...extra,
-        },
-        {
-            preserveState: true,
-            replace: true,
-            preserveScroll: true,
-            onFinish: () => {
-                loading.value = false;
-            },
-        }
-    );
-};
 
-const submitFilters = () => load({ page: 1 });
-const clearFilters = () => {
-    filterState.search = "";
-    filterState.sort_field = "name";
-    filterState.sort_direction = "asc";
-    filterState.per_page = 10;
-    submitFilters();
-};
 
-const onSort = (event) => {
-    filterState.sort_field = event.sortField;
-    filterState.sort_direction = event.sortOrder === 1 ? "asc" : "desc";
-    load({ page: 1 });
-};
-
-const onPage = (event) => {
-    filterState.per_page = event.rows;
-    load({ page: event.page + 1, per_page: event.rows });
-};
 
 const openCreate = () => {
     editModalVisible.value = false;
@@ -191,7 +174,7 @@
         return "-";
     }
 
-    return new Date(value).toLocaleString("hu-HU");
+    return dateTime(value);
 };
 </script>
 
@@ -331,7 +314,7 @@
                                     icon="pi pi-pencil"
                                     text
                                     rounded
-                                    class="!h-11 !w-11"
+                                    class="h-11! w-11!"
                                     aria-label="Beszállító szerkesztése"
                                     @click="openEdit(data)"
                                 />
@@ -340,7 +323,7 @@
                                     text
                                     rounded
                                     severity="danger"
-                                    class="!h-11 !w-11"
+                                    class="h-11! w-11!"
                                     aria-label="Beszállító törlése"
                                     @click="confirmDelete(data)"
                                 />
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/UserRoles/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/UserRoles/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/UserRoles/Index.vue	2026-05-04 14:48:22.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/UserRoles/Index.vue	2026-05-05 07:12:20.096078629 +0000
@@ -1,6 +1,6 @@
 <script setup>
 import { Head, router, useForm } from "@inertiajs/vue3";
-import { computed, reactive, ref } from "vue";
+import { computed, ref } from "vue";
 import Button from "primevue/button";
 import Column from "primevue/column";
 import DataTable from "primevue/datatable";
@@ -12,8 +12,9 @@
 import UserRoleAssignmentModal from "@/Components/Admin/UserRoles/UserRoleAssignmentModal.vue";
 import SectionTitle from "@/Components/SectionTitle.vue";
 import AdminLayout from "@/Layouts/AdminLayout.vue";
-import { perPageOptions } from "@/Utils/functions";
+import { pageOptions as createPerPageOptions } from "@/Utils/functions";
 import { trans } from "laravel-vue-i18n";
+import { useAdminFilterState } from "@/composables/useAdminFilterState.js";
 
 defineOptions({ layout: AdminLayout });
 
@@ -45,12 +46,29 @@
     roles: [],
 });
 
-const filterState = reactive({
-    search: props.filters.search ?? "",
-    per_page: props.filters.per_page ?? 15,
+const {
+    filterState,
+    sortOrder,
+    load,
+    submitFilters,
+    clearFilters,
+    onSort,
+    onPage,
+} = useAdminFilterState({
+    filters: props.filters,
+    defaults: {
+    search: "",
+    per_page: 15,
+},
+    routeName: "admin.user-roles.index",
+    loading,
+    toQuery: (state) => ({
+            search: state.search || undefined,
+            per_page: state.per_page,
+        }),
 });
 
-const perPageOptions = perPageOptions(trans, [15, 30, 50]);
+const perPageOptions = createPerPageOptions(trans, [15, 30, 50]);
 /*
 const perPageOptions = [
     { label: '15 / oldal', value: 15 },
@@ -68,38 +86,8 @@
     )
 );
 
-const load = (extra = {}) => {
-    loading.value = true;
 
-    router.get(
-        route("admin.user-roles.index"),
-        {
-            search: filterState.search || undefined,
-            per_page: filterState.per_page,
-            ...extra,
-        },
-        {
-            preserveState: true,
-            preserveScroll: true,
-            replace: true,
-            onFinish: () => {
-                loading.value = false;
-            },
-        }
-    );
-};
 
-const submitFilters = () => load({ page: 1 });
-const clearFilters = () => {
-    filterState.search = "";
-    filterState.per_page = 15;
-    submitFilters();
-};
-
-const onPage = (event) => {
-    filterState.per_page = event.rows;
-    load({ page: event.page + 1, per_page: event.rows });
-};
 
 const openAssignModal = (user) => {
     selectedUser.value = user;
@@ -242,7 +230,7 @@
                                 label="Szerepkörök kezelése"
                                 size="small"
                                 outlined
-                                class="!min-h-11"
+                                class="min-h-11!"
                                 @click="openAssignModal(data)"
                             />
                         </template>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Users/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Users/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/Users/Index.vue	2026-05-04 14:49:54.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/Users/Index.vue	2026-05-05 07:12:20.097347703 +0000
@@ -1,6 +1,6 @@
 <script setup>
 import { Head, Link, router, useForm } from "@inertiajs/vue3";
-import { computed, reactive, ref } from "vue";
+import { computed, ref } from "vue";
 import Button from "primevue/button";
 import Checkbox from "primevue/checkbox";
 import Column from "primevue/column";
@@ -21,7 +21,8 @@
 import AdminLayout from "@/Layouts/AdminLayout.vue";
 
 import { trans } from "laravel-vue-i18n";
-import { perPageOptions } from "@/Utils/functions";
+import { useAdminFilterState } from "@/composables/useAdminFilterState.js";
+import { pageOptions as createPerPageOptions } from "@/Utils/functions";
 
 defineOptions({ layout: AdminLayout });
 
@@ -49,15 +50,35 @@
 const selectedUser = ref(null);
 const selectedDiscountId = ref(null);
 
-const filterState = reactive({
-    search: props.filters.search ?? "",
-    status: props.filters.status ?? "",
-    sort_field: props.filters.sort_field ?? "created_at",
-    sort_direction: props.filters.sort_direction ?? "desc",
-    per_page: props.filters.per_page ?? 15,
+const {
+    filterState,
+    sortOrder,
+    load,
+    submitFilters,
+    clearFilters,
+    onSort,
+    onPage,
+} = useAdminFilterState({
+    filters: props.filters,
+    defaults: {
+    search: "",
+    status: "",
+    sort_field: "created_at",
+    sort_direction: "desc",
+    per_page: 15,
+},
+    routeName: "admin.users.index",
+    loading,
+    toQuery: (state) => ({
+            search: state.search || undefined,
+            status: state.status || undefined,
+            sort_field: state.sort_field,
+            sort_direction: state.sort_direction,
+            per_page: state.per_page,
+        }),
 });
 
-const perPageOptions = perPageOptions(trans, [15, 30, 50]);
+const perPageOptions = createPerPageOptions(trans, [15, 30, 50]);
 /*
 const perPageOptions = [
     { label: "15 / oldal", value: 15 },
@@ -89,7 +110,6 @@
         value: type,
     }))
 );
-const sortOrder = computed(() => (filterState.sort_direction === "asc" ? 1 : -1));
 const currentPage = computed(() => props.users.current_page ?? 1);
 const first = computed(() => (currentPage.value - 1) * (props.users.per_page ?? 15));
 
@@ -118,48 +138,7 @@
     reason: "",
 });
 
-const load = (extra = {}) => {
-    loading.value = true;
 
-    router.get(
-        route("admin.users.index"),
-        {
-            search: filterState.search || undefined,
-            status: filterState.status || undefined,
-            sort_field: filterState.sort_field,
-            sort_direction: filterState.sort_direction,
-            per_page: filterState.per_page,
-            ...extra,
-        },
-        {
-            preserveState: true,
-            preserveScroll: true,
-            replace: true,
-            onFinish: () => {
-                loading.value = false;
-            },
-        }
-    );
-};
-
-const submitFilters = () => load({ page: 1 });
-const clearFilters = () => {
-    filterState.search = "";
-    filterState.status = "";
-    filterState.sort_field = "created_at";
-    filterState.sort_direction = "desc";
-    filterState.per_page = 15;
-    submitFilters();
-};
-const onSort = (event) => {
-    filterState.sort_field = event.sortField;
-    filterState.sort_direction = event.sortOrder === 1 ? "asc" : "desc";
-    load({ page: 1 });
-};
-const onPage = (event) => {
-    filterState.per_page = event.rows;
-    load({ page: event.page + 1, per_page: event.rows });
-};
 
 const resetUserForm = () => {
     userForm.reset();
@@ -481,7 +460,7 @@
                                     icon="pi pi-pencil"
                                     text
                                     rounded
-                                    class="!h-11 !w-11"
+                                    class="h-11! w-11!"
                                     aria-label="Felhasználó szerkesztése"
                                     @click="openEdit(data)"
                                 />
@@ -491,7 +470,7 @@
                                     text
                                     rounded
                                     severity="danger"
-                                    class="!h-11 !w-11"
+                                    class="h-11! w-11!"
                                     aria-label="Felhasználó inaktiválása"
                                     @click="confirmDeactivate(data)"
                                 />
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/WeeklyMenus/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/WeeklyMenus/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Admin/WeeklyMenus/Index.vue	2026-05-04 14:52:08.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Admin/WeeklyMenus/Index.vue	2026-05-05 07:12:20.098432960 +0000
@@ -1,6 +1,6 @@
 <script setup>
 import { Head, router, useForm } from "@inertiajs/vue3";
-import { computed, reactive, ref } from "vue";
+import { computed, ref } from "vue";
 import Button from "primevue/button";
 import Column from "primevue/column";
 import ConfirmDialog from "primevue/confirmdialog";
@@ -16,7 +16,8 @@
 import SectionTitle from "@/Components/SectionTitle.vue";
 import AdminLayout from "@/Layouts/AdminLayout.vue";
 import { trans } from "laravel-vue-i18n";
-import { perPageOptions } from "@/Utils/functions";
+import { useAdminFilterState } from "@/composables/useAdminFilterState.js";
+import { pageOptions as createPerPageOptions } from "@/Utils/functions";
 
 defineOptions({ layout: AdminLayout });
 
@@ -46,12 +47,32 @@
     );
 });
 
-const filterState = reactive({
-    search: props.filters.search ?? "",
-    status: props.filters.status ?? "",
-    sort_field: props.filters.sort_field ?? "week_start",
-    sort_direction: props.filters.sort_direction ?? "desc",
-    per_page: props.filters.per_page ?? 10,
+const {
+    filterState,
+    sortOrder,
+    load,
+    submitFilters,
+    clearFilters,
+    onSort,
+    onPage,
+} = useAdminFilterState({
+    filters: props.filters,
+    defaults: {
+        search: "",
+        status: "",
+        sort_field: "week_start",
+        sort_direction: "desc",
+        per_page: 10,
+    },
+    routeName: "admin.weekly-menus.index",
+    loading,
+    toQuery: (state) => ({
+        search: state.search || undefined,
+        status: state.status || undefined,
+        sort_field: state.sort_field,
+        sort_direction: state.sort_direction,
+        per_page: state.per_page,
+    }),
 });
 
 const statusOptions = computed(() => [
@@ -59,7 +80,7 @@
     ...props.statuses,
 ]);
 
-const perPageOptions = perPageOptions(trans, [10, 20, 50]);
+const perPageOptions = createPerPageOptions(trans, [10, 20, 50]);
 /*
 const perPageOptions = computed(() => [
     { label: trans("common.page_count", { count: 10 }), value: 10 },
@@ -79,57 +100,11 @@
     is_featured: false,
 });
 
-const sortOrder = computed(() => (filterState.sort_direction === "asc" ? 1 : -1));
 const currentPage = computed(() => props.weeklyMenus.current_page ?? 1);
 const first = computed(
     () => (currentPage.value - 1) * (props.weeklyMenus.per_page ?? 10)
 );
 
-const load = (extra = {}) => {
-    loading.value = true;
-
-    router.get(
-        route("admin.weekly-menus.index"),
-        {
-            search: filterState.search || undefined,
-            status: filterState.status || undefined,
-            sort_field: filterState.sort_field,
-            sort_direction: filterState.sort_direction,
-            per_page: filterState.per_page,
-            ...extra,
-        },
-        {
-            preserveState: true,
-            replace: true,
-            preserveScroll: true,
-            onFinish: () => {
-                loading.value = false;
-            },
-        }
-    );
-};
-
-const submitFilters = () => load({ page: 1 });
-const clearFilters = () => {
-    filterState.search = "";
-    filterState.status = "";
-    filterState.sort_field = "week_start";
-    filterState.sort_direction = "desc";
-    filterState.per_page = 10;
-    submitFilters();
-};
-
-const onSort = (event) => {
-    filterState.sort_field = event.sortField;
-    filterState.sort_direction = event.sortOrder === 1 ? "asc" : "desc";
-    load({ page: 1 });
-};
-
-const onPage = (event) => {
-    filterState.per_page = event.rows;
-    load({ page: event.page + 1, per_page: event.rows });
-};
-
 const openCreate = () => {
     editModalVisible.value = false;
     editingId.value = null;
@@ -466,7 +441,7 @@
                                     icon="pi pi-list"
                                     text
                                     rounded
-                                    class="!h-11 !w-11"
+                                    class="h-11! w-11!"
                                     :aria-label="$t('admin_weekly_menus.actions.items')"
                                     @click="openItems(data)"
                                 />
@@ -474,7 +449,7 @@
                                     icon="pi pi-pencil"
                                     text
                                     rounded
-                                    class="!h-11 !w-11"
+                                    class="h-11! w-11!"
                                     :aria-label="$t('admin_weekly_menus.actions.edit')"
                                     @click="openEdit(data)"
                                 />
@@ -483,7 +458,7 @@
                                     text
                                     rounded
                                     severity="danger"
-                                    class="!h-11 !w-11"
+                                    class="h-11! w-11!"
                                     :aria-label="$t('admin_weekly_menus.actions.delete')"
                                     @click="confirmDelete(data)"
                                 />
@@ -492,7 +467,7 @@
                                     :label="$t('admin_weekly_menus.actions.publish')"
                                     size="small"
                                     text
-                                    class="!min-h-11 !text-green-700"
+                                    class="min-h-11! text-green-700!"
                                     @click="publish(data)"
                                 />
                                 <Button
@@ -500,7 +475,7 @@
                                     :label="$t('admin_weekly_menus.actions.unpublish')"
                                     size="small"
                                     text
-                                    class="!min-h-11 !text-amber-700"
+                                    class="min-h-11! text-amber-700!"
                                     @click="unpublish(data)"
                                 />
                             </div>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Cart/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Cart/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Cart/Index.vue	2026-05-04 13:49:42.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Cart/Index.vue	2026-05-05 07:13:36.089528370 +0000
@@ -4,9 +4,12 @@
 import InputNumber from "primevue/inputnumber";
 import PublicLayout from "../../Layouts/PublicLayout.vue";
 import { useConversionTracking } from "@/composables/useConversionTracking";
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
 
 defineOptions({ layout: PublicLayout });
 
+const { number } = useLocaleFormat();
+
 const props = defineProps({
     cart: {
         type: Object,
@@ -98,7 +101,7 @@
                             </p>
                             <p class="mt-2 text-sm font-semibold text-bakery-brown">
                                 {{
-                                    new Intl.NumberFormat("hu-HU").format(item.unit_price)
+                                    number(item.unit_price)
                                 }}
                                 Ft / db
                             </p>
@@ -116,20 +119,21 @@
                                     (value) => updateQuantity(item, value)
                                 "
                             />
-                            <button
+                            <Button
                                 type="button"
+                                unstyled
                                 class="text-xs font-semibold text-rose-700 hover:underline"
                                 @click="removeItem(item)"
                             >
                                 {{ $t("common.delete") }}
-                            </button>
+                            </Button>
                         </div>
                     </div>
 
                     <div class="mt-4 border-t border-bakery-brown/10 pt-4 text-right">
                         <p class="text-sm text-bakery-dark/70">Reszosszeg</p>
                         <p class="text-lg font-semibold text-bakery-dark">
-                            {{ new Intl.NumberFormat("hu-HU").format(item.line_total) }}
+                            {{ number(item.line_total) }}
                             Ft
                         </p>
                     </div>
@@ -151,7 +155,7 @@
                         <dt>{{ $t("admin_orders.columns.total") }}</dt>
                         <dd>
                             {{
-                                new Intl.NumberFormat("hu-HU").format(cart.summary.total)
+                                number(cart.summary.total)
                             }}
                             Ft
                         </dd>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Checkout/Index.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Checkout/Index.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Checkout/Index.vue	2026-05-04 13:49:42.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Checkout/Index.vue	2026-05-05 07:13:36.090840419 +0000
@@ -7,9 +7,12 @@
 import Textarea from 'primevue/textarea';
 import PublicLayout from '../../Layouts/PublicLayout.vue';
 import { useConversionTracking } from '@/composables/useConversionTracking';
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
 
 defineOptions({ layout: PublicLayout });
 
+const { number } = useLocaleFormat();
+
 const props = defineProps({
     cart: {
         type: Object,
@@ -135,16 +138,16 @@
                     <li v-for="item in cart.items" :key="item.product_id" class="flex items-start justify-between gap-3">
                         <div>
                             <p class="font-medium text-bakery-dark">{{ item.name }}</p>
-                            <p class="text-xs text-bakery-dark/70">{{ item.quantity }} x {{ new Intl.NumberFormat('hu-HU').format(item.unit_price) }} Ft</p>
+                            <p class="text-xs text-bakery-dark/70">{{ item.quantity }} x {{ number(item.unit_price) }} Ft</p>
                         </div>
-                        <p class="font-semibold text-bakery-dark">{{ new Intl.NumberFormat('hu-HU').format(item.line_total) }} Ft</p>
+                        <p class="font-semibold text-bakery-dark">{{ number(item.line_total) }} Ft</p>
                     </li>
                 </ul>
 
                 <div class="mt-5 border-t border-bakery-brown/10 pt-4 text-sm">
                     <div class="flex justify-between font-semibold text-bakery-dark">
                         <span>Végösszeg</span>
-                        <span>{{ new Intl.NumberFormat('hu-HU').format(cart.summary.total) }} Ft</span>
+                        <span>{{ number(cart.summary.total) }} Ft</span>
                     </div>
                 </div>
 
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Pages/Orders/Success.vue /mnt/data/hajnalhej_locale/resources/js/Pages/Orders/Success.vue
--- /mnt/data/hajnalhej_locale_original/resources/js/Pages/Orders/Success.vue	2026-05-04 13:49:42.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Pages/Orders/Success.vue	2026-05-05 07:13:36.091911523 +0000
@@ -1,9 +1,12 @@
 <script setup>
 import { Head, Link } from '@inertiajs/vue3';
 import PublicLayout from '../../Layouts/PublicLayout.vue';
+import { useLocaleFormat } from "@/composables/useLocaleFormat";
 
 defineOptions({ layout: PublicLayout });
 
+const { number } = useLocaleFormat();
+
 const props = defineProps({
     order: {
         type: Object,
@@ -30,7 +33,7 @@
                 </div>
                 <div>
                     <dt class="text-bakery-dark/60">Vegosszeg</dt>
-                    <dd class="font-semibold text-bakery-dark">{{ new Intl.NumberFormat('hu-HU').format(order.total) }} Ft</dd>
+                    <dd class="font-semibold text-bakery-dark">{{ number(order.total) }} Ft</dd>
                 </div>
                 <div>
                     <dt class="text-bakery-dark/60">Átvétel dátuma</dt>
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/Utils/functions.js /mnt/data/hajnalhej_locale/resources/js/Utils/functions.js
--- /mnt/data/hajnalhej_locale_original/resources/js/Utils/functions.js	2026-05-04 15:14:52.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/Utils/functions.js	2026-05-04 15:30:44.000000000 +0000
@@ -7,8 +7,8 @@
     }));
 }
 
-export function perPageOptions(trans, pages = [10, 20, 50]) {
-    return days.map((count) => ({
+export function pageOptions(trans, pages = [10, 20, 50]) {
+    return pages.map((count) => ({
         label: trans("common.page_count", { count: count }),
         value: count,
     }));
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/composables/useAdminFilterState.js /mnt/data/hajnalhej_locale/resources/js/composables/useAdminFilterState.js
--- /mnt/data/hajnalhej_locale_original/resources/js/composables/useAdminFilterState.js	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/composables/useAdminFilterState.js	2026-05-04 15:44:12.000000000 +0000
@@ -0,0 +1,89 @@
+import { computed, reactive } from "vue";
+import { router } from "@inertiajs/vue3";
+
+const cloneDefaults = (defaults) => ({ ...defaults });
+
+export function useAdminFilterState({
+    filters = {},
+    defaults = {},
+    routeName,
+    toQuery,
+    loading = null,
+    routerOptions = {},
+}) {
+    const initialDefaults = cloneDefaults(defaults);
+
+    const filterState = reactive({
+        ...initialDefaults,
+        ...Object.fromEntries(
+            Object.entries(initialDefaults).map(([key, defaultValue]) => [
+                key,
+                (defaultValue === null ? filters[key] || null : filters[key] ?? defaultValue),
+            ])
+        ),
+    });
+
+    const sortOrder = computed(() =>
+        filterState.sort_direction === "asc" ? 1 : -1
+    );
+
+    const resetFilters = () => {
+        Object.assign(filterState, cloneDefaults(initialDefaults));
+    };
+
+    const load = (extra = {}) => {
+        if (loading) {
+            loading.value = true;
+        }
+
+        router.get(
+            route(routeName),
+            {
+                ...(toQuery ? toQuery(filterState) : { ...filterState }),
+                ...extra,
+            },
+            {
+                preserveState: true,
+                replace: true,
+                preserveScroll: true,
+                ...routerOptions,
+                onFinish: (...args) => {
+                    if (loading) {
+                        loading.value = false;
+                    }
+
+                    routerOptions.onFinish?.(...args);
+                },
+            }
+        );
+    };
+
+    const submitFilters = () => load({ page: 1 });
+
+    const clearFilters = () => {
+        resetFilters();
+        submitFilters();
+    };
+
+    const onSort = (event) => {
+        filterState.sort_field = event.sortField;
+        filterState.sort_direction = event.sortOrder === 1 ? "asc" : "desc";
+        load({ page: 1 });
+    };
+
+    const onPage = (event) => {
+        filterState.per_page = event.rows;
+        load({ page: event.page + 1, per_page: event.rows });
+    };
+
+    return {
+        filterState,
+        sortOrder,
+        load,
+        submitFilters,
+        clearFilters,
+        resetFilters,
+        onSort,
+        onPage,
+    };
+}
diff -ruN /mnt/data/hajnalhej_locale_original/resources/js/composables/useLocaleFormat.js /mnt/data/hajnalhej_locale/resources/js/composables/useLocaleFormat.js
--- /mnt/data/hajnalhej_locale_original/resources/js/composables/useLocaleFormat.js	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/hajnalhej_locale/resources/js/composables/useLocaleFormat.js	2026-05-05 07:10:58.476326484 +0000
@@ -0,0 +1,69 @@
+import { computed } from "vue";
+import { usePage } from "@inertiajs/vue3";
+
+const intlLocaleMap = {
+    hu: "hu-HU",
+    en: "en-US",
+};
+
+const normalizeIntlLocale = (locale) => intlLocaleMap[locale] ?? locale ?? "hu-HU";
+
+export function useLocaleFormat() {
+    const page = usePage();
+
+    const locale = computed(() => page.props.locale ?? page.props.preferences?.locale ?? "hu");
+    const intlLocale = computed(() => normalizeIntlLocale(locale.value));
+
+    const number = (value, options = {}) =>
+        new Intl.NumberFormat(intlLocale.value, {
+            minimumFractionDigits: 0,
+            maximumFractionDigits: 3,
+            ...options,
+        }).format(Number(value ?? 0));
+
+    const currency = (value, currencyCode = "HUF", options = {}) =>
+        new Intl.NumberFormat(intlLocale.value, {
+            style: "currency",
+            currency: currencyCode,
+            maximumFractionDigits: 0,
+            ...options,
+        }).format(Number(value ?? 0));
+
+    const percent = (value, options = {}) =>
+        new Intl.NumberFormat(intlLocale.value, {
+            style: "percent",
+            minimumFractionDigits: 2,
+            maximumFractionDigits: 2,
+            ...options,
+        }).format(Number(value ?? 0) / 100);
+
+    const date = (value, options = {}) => {
+        if (!value) {
+            return "-";
+        }
+
+        return new Intl.DateTimeFormat(intlLocale.value, options).format(new Date(value));
+    };
+
+    const dateTime = (value, options = {}) => {
+        if (!value) {
+            return "-";
+        }
+
+        return new Intl.DateTimeFormat(intlLocale.value, {
+            dateStyle: "medium",
+            timeStyle: "short",
+            ...options,
+        }).format(new Date(value));
+    };
+
+    return {
+        locale,
+        intlLocale,
+        number,
+        currency,
+        percent,
+        date,
+        dateTime,
+    };
+}
